// Content-build-only stub. Run `dotnet build` to compile all fixture shaders via MGCB.
// No game loop — this project exists solely to drive mgfxc on the fixture corpus.
static class Program { static void Main() { } }
