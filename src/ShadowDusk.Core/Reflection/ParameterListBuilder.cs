#nullable enable

// Intentionally empty — see ShadowDusk.HLSL.Reflection.ParameterListBuilder,
// which lives in the HLSL project to avoid a circular assembly reference.
// (ParameterListBuilder requires ShadowDusk.HLSL.Ast.ParameterAnnotation.)
namespace ShadowDusk.Core.Reflection;
