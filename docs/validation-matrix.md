# ShadowDusk Validation Matrix

**Purpose:** one place to *definitively* track what ShadowDusk is proven to do, across every runtime
library, shader format/version, graphics target, and OS, and to mark cells off as they advance. This is a
**living checklist**: update a cell's status (and the date) whenever its evidence changes.

**Last updated:** 2026-07-31 — the DX12 Apos.Shapes gallery's 1/255 delta is **root-caused** (the pinned DXC build, not a ShadowDusk defect) and the gallery harness gained two corrections; see the entry below. Earlier, 2026-07-30 refreshed for the 0.16.0 release: the CI GL-gate list is now **seven** gates (below), and the `ForwardCompat` history entry reflects the full seven-release sweep. Earlier still, 2026-07-27 corrected the §6 DX11/DX12 Apos.Shapes gallery rows to the
**Last updated:** 2026-07-31 — Phase 51 A10: the ShaderToy route's DirectX arm exists (`validation/ShaderToyRouteDx`, wired into the Windows gate script, **not yet run**), and the DirectX target now enforces mgfxc's compile-profile floor (`SD0015`); §8.1 records the measured per-target floors. Previously, 2026-07-30 — refreshed for the 0.16.0 release: the CI GL-gate list is now **seven** gates (below), and the `ForwardCompat` history entry reflects the full seven-release sweep. Earlier, 2026-07-27 corrected the §6 DX11/DX12 Apos.Shapes gallery rows to the
2026-07-23 oracle-arm switch: the gallery gates diff against **real `mgfxc` goldens** (the
package's embedded effect turned out to be vkd3d-shader-compiled, so it now serves only as the
DX11 vkd3d arm's same-toolchain baseline). What changed recently:

- **2026-07-31 — DX12 Apos.Shapes 1/255 root-caused: it is the DXC pin.** Ours is
  `dxcoob 1.7.2212.40 (e043f4a12)` (the `Vortice.Dxc` 3.3.4 pin), the `mgfxc` golden's is
  `dxcoob 1.8.2505.32 (b106a961d)` (MonoGame 3.8.5's bundled DXC). Feeding ShadowDusk's **own**
  pre-parsed HLSL and **own** DXC flags to a DXC 1.8 build reproduces the golden's DXIL
  instruction-for-instruction, and rendering that payload in ShadowDusk's own container gives
  **maxd 0, 0 differing pixels of 402,984**. Not a defect; `maxd 1` is the honest tolerance while
  the pins differ. Two harness corrections shipped alongside it: the per-cell breakdown named the
  wrong shape (it used untransformed layout rectangles under a 1.15x view), and a third of the
  gallery was clipped off the render target and compared nothing. See §7 and
  `plan/DONE/PHASE-55-apos-shapes-shape-gallery-render-proof.md` §8.
- **2026-07-23 — Phase 55:** the Apos.Shapes render-proofs were upgraded from one hand-built shape
  to the full 30-cell `ShapeBatch` shape gallery through the real `Apos.Shapes` NuGet package.
  DX11 (both DXBC backends) and Vulkan: **maxd 0**; DX12: maxd 0 everywhere except a handful of
  1/255 pixels vs the local `mgfxc` golden (root-caused 2026-07-31, above); GL:
  candidate-only visibility check (no trustworthy `mgfxc` GL oracle — a confirmed MojoShader bug).
  See the §6 gallery rows and `plan/DONE/PHASE-55-apos-shapes-shape-gallery-render-proof.md`.
- **2026-07-23 — Phase 54:** DirectX 12 (`WindowsDX12`, profile 2, SM6 DXIL) fully rung-4 proven —
  maxd 0 vs a real `mgfxc` `DirectX_12` golden for both the PS/SpriteBatch corpus and
  VS-driven/Apos.Shapes on real MonoGame 3.8.5. See the §1 DirectX 12 cell and `validation/VsDrivenDx12`.
- **2026-07-23 — Phase 51 A3 + issue #149:** Apos.Shapes single-shape render-proofs closed on DX
  (maxd 0, both DXBC backends) and GL (maxd 2/255, the older `apos-shapes.fx` pin); the GL
  `isnan()`/macOS gap (issue #149) found alongside is fixed.
- **2026-07-22 — issue #145 / Phase 53:** first Vulkan pixel-diff vs an `mgfxc` golden (maxd 0,
  explicit-register effects); the GL portability lint (SD0400–SD0402) landed; Phase 53 closed with
  the Windows render gates green.

Prior updates: [Update history](#update-history) at the bottom.

---

## How to read a cell (the evidence levels, in plain English)

Every cell is at one of these levels. "Proven" always means **compared to the official reference compiler
(`mgfxc` for MonoGame/KNI, `fxc /T fx_2_0` for FNA), same graphics backend, same scene.**

| Mark | Level | Plain meaning |
|---|---|---|
| ✅ | **Render-proven** | Actually rendered in the **real engine** and the picture matches the reference compiler, pixel-for-pixel. The strongest proof. |
| 🟦 | **Load-proven** | The real engine **loads** the output without error, but the rendered picture has not yet been compared. |
| 🟡 | **Compile-only** | ShadowDusk **produces well-formed output**, but no real engine has loaded or rendered it. |
| 🌐 | **Browser-proven (dated)** | Rendered in a real **browser** runtime (KNI WebGL / Blazor) and compared, but the proof predates a current engine version, treat as stale until re-run. |
| 🚫 | **Blocked** | No shipping runtime exists to validate against yet (e.g. KNI has no Vulkan; "modern GLSL" has no GL runtime). Not a defect, a dependency. |
| ⬛ | **Not implemented** | ShadowDusk does not target this yet. |
| — | **N/A** | The combination does not exist (e.g. FNA is not MGFX-versioned). |

> **Why most of the naive 3 x 4 x 4 x 4 cross-product is not real:** FNA uses one fx_2_0 `.fxb` for *all* its
> backends (not MGFX, not v10/v11). KNI ships no Vulkan, Metal, or DX12; FNA3D has no D3D12 backend.
> ShadowDusk has no Metal target yet; DX12 is now fully rung-4 proven (PS corpus + VS-driven, see
> §1). The tables below show only the **valid** combinations.

---

## 1. Primary matrix, library x graphics target

What ShadowDusk emits for each runtime/target, and the best proof to date. (Format in parentheses.)

| Runtime | DirectX (DX11) | DirectX 12 | OpenGL / GLES | Vulkan | Metal |
|---|---|---|---|---|---|
| **MonoGame** | ✅ DXBC SM5 (MGFX v10) — rendered on Windows vs `mgfxc` + `fxc` oracle (PS corpus + VS-matrix). **Apos.Shapes (Gum's SDF shape renderer) render-proven 2026-07-23** (Phase 51 A3, DX slice): `apos-shapes-sm6.fx` — the same fixture the Vulkan proof uses — pixel-diffs **maxd 0** against the `mgfxc` DirectX_11 golden on BOTH DXBC backends (`d3dcompiler_47` oracle and `vkd3d-shader`) via `validation/VsDrivenDx -- apos` | ✅ **SM6 DXIL (MGFX v11, profile 2)** ([Phase 54](../plan/DONE/PHASE-54-dx12-dxil-backend.md), split from Phase 52 Area D once source inspection showed it's a new backend). `PlatformTarget.DirectX12` compiles via DXC to plain SM6 DXIL (no `-spirv`, no `-Zpr`, no root-signature embedding — confirmed the real mgfxc golden's DXIL carries no `RTS0` part at all) reusing the existing DXIL reflection pipeline, wrapped in a small `0xB00B00`+2-int header (`DirectX12ShaderCodeWrapper`) matching real MonoGame 3.8.5's `DirectX12ShaderProfile`. **Fully rung-4 proven 2026-07-23, both corpora**: a real `mgfxc` `DirectX_12` golden (built via `dotnet-mgcb` 3.8.5's own content pipeline, `/platform:WindowsDX12`) pixel-diffs **maxd 0** against ShadowDusk's output on real MonoGame 3.8.5 `WindowsDX12` — the 10-shader PS/SpriteBatch corpus (`validation/BaselineDx12` + `CandidateDx12` + `compare_dx12.py`, 10/10) **and** the Apos.Shapes/VS-driven corpus (`validation/VsDrivenDx12` + `-- apos`). The VS-driven path previously crashed real `WindowsDX12` at draw time (`E_INVALIDARG`/`0x80070057` in `MGG_GraphicsDevice_DrawIndexed`); root-caused by reading MonoGame's actual current v3.8.5 source (not a stale clone) to a DX12 vertex shader shipping an **empty vertex-attribute table** — MonoGame's shared `VertexInputLayout.GenerateInputElements` (every native backend, not just DX12) needs that table to build the D3D12 input layout, and an empty one silently yields a zero-element layout that fails `CreateGraphicsPipelineState` at the first Draw. Fixed via `DxilVertexInputReflector` | ✅ GLSL (MGFX v10) — rendered on Linux (Mesa) + Windows vs `mgfxc` (PS corpus + VS-driven). **Apos.Shapes (Gum's SDF shape renderer) render-proven 2026-07-23** (Phase 51 A3, GL slice): `apos-shapes.fx` (the Phase 49 pin — deliberately NOT the same `apos-shapes-sm6.fx` fixture the DX/Vulkan proofs use; the real mgfxc OpenGL golden's compiled output for that revision is confirmed wrong, unrelated to ShadowDusk — see `validation/VsDriven`'s `AposShapesRenderer` remarks) pixel-diffs at **maxd 2/255** (documented transcendental-math GLSL-dialect drift, not a structural mismatch) against the mgfxc OpenGL golden via `validation/VsDriven -- apos` | ✅ SPIR-V (MGFX v11, profile 80) — **render-proven on real MonoGame 3.8.5 DesktopVK** on two evidence arms. (a) **Pixel-diff vs the `mgfxc` 3.8.5 golden — maxd 0**, on a VS-driven non-identity asymmetric transform and on the upstream `Apos.Shapes` reproducer, via `validation/VsDrivenVulkan` (issue #145, 2026-07-22); both arms mutation-proven. (b) The 10-shader PS corpus is **own-output** render-proven via `validation/CandidateVulkan` (Phase 32, 2026-07-18) — no mgfxc oracle there, because mgfxc's output crashes on load for **auto-numbered** resources (a confirmed, independent MonoGame `SlotOffset` bug). Giving a fixture **explicit registers** keeps that arithmetic in range, which is exactly how arm (a) obtains a loadable baseline; see `plan/DONE/PHASE-32-appendix/vulkan-mgfx-format-spec.md` | ⬛ MSL — ShadowDusk Metal target is a **stub** |
| **KNI** | ✅ DXBC SM5 (MGFX v10) — **render-proven on KNI v4.2.9001 (WinForms.DX11)**: ShadowDusk's DX output loads + renders **pixel-equivalent to the mgfxc DX golden (maxd <= 1, driver rounding on Dots only)** across the 10-shader corpus in real KNI DX11 (`validation/KniWinFormsDX`, 2026-06-15) | — KNI ships **no DX12** platform | ✅ GLSL (MGFX v10) — **render-proven on KNI v4.2.9001 desktop (SDL2.GL)**: v10 loads + renders **pixel-identical to MonoGame (maxd 0)** and **within maxd 1 of the mgfxc goldens** across the 10-shader corpus (`validation/KniDesktopGL`, 2026-06-14). Also **browser-proven on the current KNI v4.2.9001.2 WebGL/Blazor runtime** (refreshed 2026-06-15: 10/10 of ShadowDusk's own `.mgfx` load + render in real KNI WebGL Reach **and** HiDef/WebGL2, within the Phase-17 §6.1 tolerance vs the DesktopGL render of the same bytes; `tests/ShadowDusk.BrowserTests`) | — KNI ships **no Vulkan** platform | — KNI iOS uses **GL**, no Metal |
| **FNA** | ✅ — *one* fx_2_0 `.fxb` serves **all** FNA3D backends (D3D11 / Vulkan / OpenGL / Metal) via FNA3D + MojoShader; rendered vs `fxc /T fx_2_0` (PS + VS-driven) | — FNA3D has **no D3D12** backend | ✅ (same `.fxb`) | ✅ (same `.fxb`) | ✅ (same `.fxb`) |

**Reading it:** the rock-solid, render-proven cells today are **MonoGame OpenGL, MonoGame DirectX, FNA
(all backends), KNI OpenGL on the current v4.02 desktop runtime** (`validation/KniDesktopGL`, 2026-06-14),
**and now KNI DirectX on the v4.02 WinForms.DX11 runtime** (`validation/KniWinFormsDX`, 2026-06-15). The
remaining honest gap is the **modern DirectX features** (next section). The KNI WebGL proof was
refreshed on the current v4.02 runtime on 2026-06-15 (Reach + HiDef, 10/10). **MonoGame Vulkan
(DesktopVK)** joined the render-proven set on 2026-07-18 (Phase 32) and **gained a real
reference-compiler oracle on 2026-07-22** (issue #145): `validation/VsDrivenVulkan` pixel-diffs
ShadowDusk against the `mgfxc` 3.8.5 golden at **maxd 0**. The asterisk that remains is narrower
than it was — mgfxc's Vulkan output still can't render for **auto-numbered** resources (a separate,
confirmed MonoGame bug), so the Phase 32 PS corpus stays own-output-proven; fixtures with
**explicit registers** keep mgfxc's slot arithmetic in range and do get the full pixel-diff. **DirectX 12 (`WindowsDX12`, new in
MonoGame 3.8.5) is now a ✅ fully rung-4 proven target** ([Phase 54](../plan/DONE/PHASE-54-dx12-dxil-backend.md)) —
both the PS/SpriteBatch corpus and VS-driven effects (including Apos.Shapes) pixel-diff maxd 0
against a real `mgfxc` `DirectX_12` golden on real MonoGame 3.8.5 `WindowsDX12`. VS-driven
previously crashed at draw time; root-caused (a missing vertex-attribute table MonoGame's shared
input-layout code needs) and fixed. See the MonoGame/DirectX12 cell in §1 and `validation/VsDrivenDx12`.

## 2. Shader format / version / graphics profile

> **Format roadmap (committed):** ShadowDusk will emit **v10 *and* v11 *and* KNIFX**, each a real, faithful
> output a consumer can select and use, not just v10 with a promise that newer runtimes still load it. v10
> stays the **default** because it loads on every MGFX-lineage runtime (the seamless baseline), but v11 and
> KNIFX are **first-class additive outputs** so consumers can use the newer containers' features (and take the
> bytes for their own runtime if they like). Forward-compatibility of v10 is a nice-to-have, **not** a reason
> to stop short of v11/KNIFX. The newer formats are opt-in or auto-selected from the target, **never a flag a
> consumer must set to get correct output** (the seamless rule still holds). **All three are now BUILT and
> render-proven (2026-06-14):** v10 (default, everywhere), **MGFX v11** (`MgfxVersion = 11`, render-proven in
> MonoGame 3.8.5), and **KNIFX v11** (`Container = Knifx`, render-proven in KNI 4.02). **The
> auto-select/override seam is now built too:** a closed `CapabilityProfile` set names each
> (runtime, format) contract, `CompilerOptions.Profile` (and CLI `--target-runtime`) selects one (a
> profile implies its backend), and `RuntimeProfileDetector` recommends a proven profile from the
> loaded framework assembly, all byte-identical when unset. A `ShaderFeatures` axis
> (`ShaderFeatureSupport.Validate`) **rejects loudly with `SD0201`** any shader that needs a capability
> no shipping runtime supports yet (e.g. vertex texture fetch / texture arrays on the GL/MojoShader
> path), so an unsupported feature fails at compile rather than silently mis-rendering. KNIFX
> `columnsActual` is now **validated
> against a KNIFXC golden** (full matrices match exactly; the partially-used-matrix divergence is
> render-safe, a storage-only difference, see the KNIFX spec).

| Format / profile | Applies to | Status | Notes |
|---|---|---|---|
| **MGFX v10** | MonoGame, KNI | ✅ | The default and the basis of every ✅ above. The one container every MGFX-lineage runtime loads. |
| **MGFX v11** (MonoGame) | MonoGame 3.8.5+ | ✅ **render-proven (MonoGame 3.8.5)** | ShadowDusk emits a **faithful MGFX v11** via `CompilerOptions.MgfxVersion = 11`: the v10 body plus the two per-shader diagnostic strings (`SourceFile`, `Entrypoint`) MonoGame PR #8813 added. **Render-proven 2026-06-14**: loads + renders **10/10 in real MonoGame 3.8.5.0** (`validation/MonoGameV11`), **maxd 0 vs v10**, <= 1 vs the mgfxc goldens. **Opt-in only** (the default stays v10 because it is the most backwards-compatible container: a v10 `.mgfx` loads in 3.8.2 through 3.8.5 *and* KNI, while v11 needs 3.8.5+). The old header-byte `--mgfx-version 11` was actually **corrupt** (v10 body + version 11 desyncs a v11 reader); now fixed. **The Phase 45 B10 reserved-word free-uniform binding is also render-proven through v11 (2026-06-18):** `float noise;` (a GLSL reserved word, SPIRV-Cross-renamed `_noise` on the GL path) stays exposed under its original name and exactly drives the output (`noise=0.25 -> grey 64`, `0.75 -> 191`, both `dExpected 0`; differentiation 127) in real MonoGame 3.8.5.0 via `validation/MonoGameV11 -- reservedword` (the B10 fix lives in the shared GL container path, so it flows into v11). Spec: [`DONE/PHASE-35-appendix/mgfx-v11-format-spec.md`](../plan/DONE/PHASE-35-appendix/mgfx-v11-format-spec.md). |
| **KNIFX v11** (KNI) | KNI 4.02+ | ✅ **render-proven (corpus); feature parity pending** | ShadowDusk **emits KNIFX v11** today (`CompilerOptions.Container = Knifx`, `KnifxWriter`): signature `KNIF`, multi-backend directory, packed-int body, GL GLSL-version directory. Still **opt-in / experimental** (not auto-selected yet). Render-proven in real KNI v4.2.9001: **desktop SDL2.GL 10/10, maxd 0 vs the v10 render** (2026-06-14, `validation/KniDesktopGL knifx`); **KNI WebGL** via the multi-backend GL-family directory — one `.knifx` serves desktop GL + GLES + WebGL (2026-06-27, detail in the §7 "KNIFX container on KNI WebGL" row); the B10 reserved-word free-uniform binding (2026-06-18, §6). A KNIFX compile defines `__KNIFX__=1`, matching KNI's compiler, so KNI shaders that branch on it take the correct branch (2026-06-27); the universal MGFX default stays `__KNIFX__`-free. `columnsActual` is **validated against a KNIFXC golden** — the partially-used-matrix divergence is render-safe (see the format-roadmap note above; the sampler-without-texture fix is not golden-checked yet). KNIFX = the new container + parity fixes over a still-MojoShader body. Spec: [`DONE/PHASE-35-appendix/knifx-format-spec.md`](../plan/DONE/PHASE-35-appendix/knifx-format-spec.md). |
| **Reach** (WebGL1 / GL ES 1.00) | MonoGame, KNI GL | ✅ | The default GL output dialect. |
| **HiDef** (WebGL2 / GL ES 3.00) | KNI GL | 🌐 (current) | The `#version 300`-guarded output; **browser-proven on KNI v4.2.9001.2** (refreshed 2026-06-15, `--corpus=sd-hidef`): 10/10 of ShadowDusk's own `.mgfx` load + render in a real KNI HiDef/WebGL2 context, and the issue-#7 `gl_FragColor` regression guard holds GREEN. |

## 3. OS coverage (where the proof actually ran)

ShadowDusk's **output bytes are OS-independent** (proven byte-identical on Windows/Linux/macOS by
`CrossHostByteIdentityTests`), so compiling is cross-OS-solved. What varies by OS is where a **render** was
actually run.

> **One carve-out: `DirectX12` output is host-dependent.** The byte-identity manifest covers
> `DirectX_Vkd3d`, `FNA`, and `OpenGL` only. DXIL validation/signing runs through the Windows-only
> `dxil.dll`, so a DX12 compile on Linux/macOS emits **unsigned** DXIL that retail D3D12 rejects at
> pipeline-state creation — a compile-time `SD0214` warning, not an error. DX12 content must be
> built on Windows until cross-platform signing ships (see §7).

| OS | Compile | Render proof that ran here |
|---|---|---|
| **Windows** | ✅ (CI) | ✅ DirectX (validation harnesses) + ✅ FNA (`fxc` oracle) + ✅ **KNI OpenGL desktop** (`validation/KniDesktopGL`, real SDL2.GL driver). MonoGame GL render **soft-skips** in CI (runners expose only GDI Generic GL), but renders on a real desktop driver. |
| **Linux** | ✅ (CI, byte-identical) | ✅ OpenGL (Mesa software GL, the `ShadowDusk.ImageTests` suite runs in CI here). |
| **macOS** | ✅ (CI, byte-identical) | (no separate render run — transferred via byte-identity: the output equals the Windows/Linux bytes, so their render proofs carry over). |
| **Web (WASM / Blazor)** | ✅ (in-browser DXC+SPIRV-Cross frontend, Phase 23) | 🌐 (current) KNI WebGL (Phase 24 Playwright harness) **refreshed on KNI v4.2.9001.2, 2026-06-15** (Reach + HiDef, 10/10). |
| **Android** (on-device compile) | 🟢 **compile-proven on-device (Phase 50, 2026-06-28)** — NDK-built DXC + SPIRV-Cross (`arm64-v8a` + `x86_64`), seamless `new EffectCompiler()` | 🟦 **load-proven on a real `pixel_7` API-34 emulator**: an HLSL string compiled **in memory, on the device** → 410-byte `.mgfx` → loaded into a live MonoGame `Effect` (`validation/AndroidGl`, green-screen success). DXC cross-compiled for Android via `.wasm-build/build-dxc-android.ps1`; `EffectCompiler` auto-selects the managed `SpirvReflector` on Android (byte-identical to DXIL). Remaining rung: on-device **pixel-vs-`mgfxc` render diff** (harness today proves compile + `Effect` load, not a pixel compare). Build-time precompile also works (OS-independent bytes). |

## 4. The modern-DirectX-features sub-checklist (called out because it is a live gap)

The DirectX target is **not** MojoShader-limited (that limit is OpenGL-only). It already *compiles* the SM4/5
features the GL path rejects, but their **render** is not yet proven.

| Feature | Compiles (DirectX) | Rejected on OpenGL | Rendered + matched on DirectX |
|---|---|---|---|
| Vertex texture fetch (`SampleLevel` in VS) | ✅ (exit 0) | ✅ `SD0210` (correct) | ✅ **render-proven** — vkd3d == `fxc` oracle at **maxd 0** in real MonoGame WindowsDX, VTF actually deforms the mesh (`validation/DxModernFeatures`, 2026-06-14) |
| `Texture2DArray` | ✅ (exit 0) | ✅ `SD0210` (correct) | ✅ **our part done** / render **N/A (not our gap)** — ShadowDusk compiles it to valid DXBC (pinned by `ValidationMatrixCoverageTests`); a render can't be set up because **MonoGame exposes no public `Texture2DArray` binding API**. A MonoGame runtime limitation, **closed from ShadowDusk's side**; revisit only if MonoGame adds array binding. |

VTF is closed (render-proven). The texture-array render is **N/A from ShadowDusk's side** — our compile is
correct and pinned; the render simply can't be exercised because MonoGame has no public array-binding API, a
MonoGame limitation rather than a ShadowDusk gap. The compile rung for both is pinned by
`ValidationMatrixCoverageTests`; VTF render by `validation/DxModernFeatures`.

---

## 5. How a cell advances (the workflow to turn 🟡/🟦 into ✅)

1. **Compile** the fixture to the target (already automated, see below).
2. **Load** it in the real engine (`new Effect(gd, bytes)` for MonoGame/KNI; FNA `Effect`).
3. **Render** a frame and pixel-compare to the reference compiler's output for the *same backend + scene*
   (tolerance is the established bar: max per-channel delta <= 1 for dyadic inputs).
4. Mark the cell ✅ with the date and the test/harness that proves it.

## 6. Programmatic tests that back each cell (what exists today)

| Coverage | Test / harness | Automated in CI? | How to run locally |
|---|---|---|---|
| Compile every fixture to DX + GL (+ census) | `tests/ShadowDusk.Integration.Tests` `Phase41StructuralDivergenceMatrixTests` | ✅ | `dotnet test ...Integration.Tests --filter Phase41StructuralDivergence` |
| **FX language-construct parse regression** (relational operators, ternaries, shifts, helper functions in shader bodies; issue #106 `FX0001` false positive) | `tests/ShadowDusk.HLSL.Tests` (`FxPreParser` unit cases) + the regression `.fx` fixture corpus compile | ✅ | `dotnet test tests/ShadowDusk.HLSL.Tests` (full corpus: `dotnet test ShadowDusk.slnx`) |
| **Matrix compile/reject claims** (this doc's compile rung: GL rejects VTF/arrays `SD0210`, DX compiles them, FNA rejects SM4 `SD0300`) | `ValidationMatrixCoverageTests` | ✅ | `dotnet test ...Integration.Tests --filter ValidationMatrixCoverage` |
| **Vulkan structural invariants over the WHOLE corpus** (issue #145): every `.fx` fixture must either compile to a container MonoGame's DesktopVK can actually bind — combined image-samplers at binding ≥ 32, unique binding numbers, uniform buffer at 0, HLSL column-major matrices, `main` entry point, no `SPV_GOOGLE_*` extensions — **or fail with a real diagnostic** (never an exception, never a native crash). Device-free, so it runs everywhere; catches both #145 bugs plus the cube/3D unpaired-sampler variant with no GPU. No skip list; a guard test fails if the enumeration shrinks. | `VulkanCorpusStructuralTests` | ✅ | `dotnet test ...Integration.Tests --filter VulkanCorpusStructural` |
| **MonoGame's own test effects** (Ms-PL, tag `v3.8.5`, `third-party/MonoGame/`) — the reference compiler's per-profile acceptance set (upstream `Vulkan.mgcb` / `DirectX.mgcb`), incl. `Instancing.fx` (VS-driven, `float4x4` vertex input), `VertexTextureEffect.fx`, `ParameterTypes.fx`, `ParserTest.fx`. Compile-level classification per target, recorded in that directory's `NOTICE.md`. | `Phase41StructuralDivergenceMatrixTests` auto-glob + `VulkanCorpusStructuralTests` | ✅ | `dotnet test ShadowDusk.slnx` |
| **Third-party vendored corpus** (real shipping shaders compile to a well-formed container on their classified targets): Nez (MIT) + **Gum / Apos.Shapes (MIT, Phase 49)** — incl. `apos-shapes.fx` Gum SDF renderer on GL+DX. Compile-level, not pixel-equivalence (no goldens for these). | `ThirdPartyShaderCorpusTests` (+ `FnaCompileFixtureTests.Sm3Corpus`, `Phase41StructuralDivergenceMatrixTests` auto-glob) | ✅ | `dotnet test ...Integration.Tests --filter ThirdPartyShaderCorpus` |
| **Macro-defined techniques (`TECHNIQUE()` idiom) recovered** — DX (existing) + **FNA (Phase 41 GAP-1, 2026-06-27)**: the SM2-fitting MonoGame stock effects compile on FNA to valid fx_2_0; BasicEffect/SkinnedEffect → honest `SD0305` (SM2 register pressure), Gum FnaSample → `SD0300` (sub-SM2 `vs_1_1`). Compile-level (the 7 new FNA effects are not render-proven vs `fxc` goldens). | `Phase41MacroTechniqueTests` (DX + FNA cases) | ✅ | `dotnet test ...Integration.Tests --filter Phase41MacroTechnique` |
| **Cross-OS byte-identical** output (Win/Linux/Mac; GL/DX/FNA) | `CrossHostByteIdentityTests` | ✅ (all 3 OSes) | `dotnet test ...Integration.Tests --filter CrossHostByteIdentity` |
| OpenGL render vs golden (software GL) | `tests/ShadowDusk.ImageTests` (incl. `MatrixConventionSweepTests`, `Issue70MatrixTransposeRenderTests`) | ✅ (Linux Mesa) | `dotnet test tests/ShadowDusk.ImageTests` |
| **Real MonoGame OpenGL** render vs `mgfxc` | `validation/VsDriven`, `validation/Baseline` (mgfxc golden arm) + `validation/Candidate` + `validation/compare.py` | manual | `dotnet run --project validation/VsDriven` for the VS rig; the CORPUS comparison needs all three: `dotnet run --project validation/Baseline`, then `… /Candidate`, then `python validation/compare.py` |
| **Real MonoGame OpenGL Apos.Shapes render-proof** (Phase 51 A3, GL slice — Gum's SDF shape renderer). Uses `apos-shapes.fx` (the Phase 49 pin, upstream commit `3fb73b8d`) — deliberately NOT `apos-shapes-sm6.fx` (the DX/Vulkan fixture): that revision compiles fine on GL, but the real mgfxc OpenGL golden's compiled output diverges completely (maxd 255, solid black) for a confirmed reason unrelated to ShadowDusk — MojoShader's GL translation of that revision's fxc-optimized shape dispatch hinges on a `-0.0 >= 0.0` comparison this GPU/driver evaluates false, permanently selecting a hard-zeroed color branch (an independent double-precision recomputation of the shader's OkLab math confirms ShadowDusk's candidate is the mathematically correct one). `apos-shapes.fx`'s plain sequential shape dispatch + Cantor-pair color packing sidesteps it. Renders through a bespoke 10-element vertex-buffer harness (`POSITION0, TEXCOORD0-8`) with the same non-identity asymmetric transform (issue #70 discipline) DX/Vulkan use. Pixel-diffs at **maxd 2/255** (216/16384 pixels, concentrated at 1-2/255 — the documented transcendental-math GLSL-dialect drift between SPIRV-Cross and MojoShader on the shader's OkLab round-trip, not the maxd-0 bar DX/Vulkan hit) against the real mgfxc `OpenGL` golden. **Since issue #160 (2026-07-25) also renders a needle-thin ellipse** (aspect ~18:1, shape 6) as a second same-backend slice — supplementary coverage for the header-less-loop rewrite; because that bug was an uninitialized read (UB) whose pixel effect is driver-dependent, this slice can stay green on a driver that masks the UB (e.g. this box, maxd 1), so the authoritative guard is the rewriter unit test `PixelStage_BoundedHeaderlessForLoop_ElseBranchStillReachableAtMaxTripCount_Issue160`. | `validation/VsDriven -- apos` (in-process compare, `AposShapesRenderer`) | manual — **default-ON in `run-windows-render-gates.ps1`** (2026-07-23) | `dotnet run -c Release --project validation/VsDriven -- apos`, or the gate script |
| **Real MonoGame OpenGL Apos.Shapes FULL SHAPE-GALLERY render-proof** (Phase 55, 2026-07-23). Renders every `ShapeBatch` public `Draw*`/`Fill*`/`Border*` shape method (circle, rectangle+corner-radii, line, path, hexagon, triangle, ellipse, arc, ring — 30 cells) through the real `Apos.Shapes` NuGet package's effect-injection `ShapeBatch(GraphicsDevice, Effect?)` constructor, driving ShadowDusk's GL compile of `apos-shapes-sm6.fx`. **No golden arm**: `ShapeBatch`'s vertex layout is identical on every backend, so GL is stuck driving this same current shader revision, and `mgfxc`'s own GL compile of it is the SAME confirmed MojoShader bug as the row above (solid black for every non-textured shape) — no trustworthy GL oracle exists for this gallery. Asserts all **30/30 shapes render visible (non-black, non-transparent) content**. Does not replace or affect the single-shape row above. | `validation/VsDriven -- apos-gallery` (candidate-only, `AposGalleryRenderer`) | manual — **default-ON in `run-windows-render-gates.ps1`** (2026-07-23) | `dotnet run -c Release --project validation/VsDriven -- apos-gallery`, or the gate script |
| **Real MonoGame OpenGL** cube + 3D/volume texture render (rung-4; cube full, 3D single-voxel) | `validation/TextureBreadthValidation` (in-process assert) | ✅ (`validation-render.yml`, ubuntu/llvmpipe) | `dotnet run --project validation/TextureBreadthValidation` |
| **Real MonoGame OpenGL** pass render-states / annotations / baked sampler-states load + render vs `mgfxc` golden (Phase 43) | `validation/StateFidelity` (in-process compare) | ✅ (`validation-render.yml`, ubuntu/llvmpipe) | `dotnet run -c Release --project validation/StateFidelity` |
| **Real MonoGame OpenGL** cbuffer + array-parameter-by-name model load + render vs `mgfxc` golden (Phase 43C) | `validation/CbufferModel` (in-process compare) | ✅ (`validation-render.yml`, ubuntu/llvmpipe) | `dotnet run -c Release --project validation/CbufferModel` |
| **Real MonoGame OpenGL** reserved-word free uniform (`float noise;`) binds by name + drives output on the B10 offset-bridge path (Phase 45 B10; exact expected pixels, plus pixel-equiv vs `mgfxc` when present) | `validation/ReservedWordGl` (in-process assert) | ✅ (`validation-render.yml`, ubuntu/llvmpipe) | `dotnet run -c Release --project validation/ReservedWordGl` |
| **Real MonoGame OpenGL** per-(texture, sampler)-PAIR sampler records (Phase 51 A7). **Arm A** (`SharedSamplerPair.fx`): two textures through ONE shared `SamplerState`, rendering an ASYMMETRIC function of the two (`float4(diffuse.r, light.g, 0, 1)`) so the outcome names the failure — **yellow = correct**, red = `ps_s1` got no texture unit and read unit 0 (the pre-A7 bug), black = the pairs are swapped. A symmetric `diffuse * light` would render identically under a swap and prove nothing. Also pixel-diffs vs the `mgfxc` `OpenGL` golden when one is committed (the arm reports which mode it ran, so a missing golden cannot pass as a diff). **Arm B** (`SamplerPairMirror.fx`): two textures with IDENTICAL pixels through a Point and a Linear `sampler_state`, sampled in the REVERSE of their declaration order, so only a correctly baked per-pair state applied in the right order can make the channels differ (R snapped, G blended). ⚠️ **Arm B was once described as proving that our `ps_s{k}` numbering divergence from `mgfxc` was benign. It never proved that, and the claim was retracted on 2026-08-02 (issue #189).** Its two textures hold IDENTICAL pixels — deliberately, so the arm isolates per-pair sampler *state* — which is exactly what makes it blind to which texture unit each pair lands on. With DISTINCT textures, and with `SpriteBatch` owning unit 0, the numbering is plainly visible in the picture. Slots now follow HLSL declaration order like fxc's; the claim is held by `validation/SamplerRegisterOrderGl` below, and arm B's narrower (real) claim is that the per-pair baked state matches the reference compiler in the same scene. Two harness details are load-bearing and were both measured, not assumed: arm B needs **distinct texture objects per unit** (three, including the sprite) because MonoGame 3.8.2's GL backend has no sampler objects and applies filtering with `glTexParameteri` on the bound **texture**, so one object on two units collapses to whichever filter was applied last (with the sprite object reused as a parameter the `mgfxc` arm rendered (255,255,0)); and the fixture must use legacy `sampler_state` rather than a modern `SamplerState { MinFilter = … }` block, which `mgfxc` does not bake at all. The one-texture-two-samplers shape is therefore un-renderable as a distinction in this runtime and is pinned at record level by `OpenGl_OneTextureTwoSamplers_BakesEachPairsOwnSamplerState` instead. | `validation/SamplerPairsGl` (in-process compare, both arms vs the real `mgfxc` `OpenGL` goldens) | ✅ (`validation-render.yml`, ubuntu/llvmpipe) | `dotnet run -c Release --project validation/SamplerPairsGl` |
| **Real MonoGame OpenGL sampler SLOT allocation follows HLSL declaration order** (GitHub issue #189) — `SamplerRegisterOrder.fx` declares `SpriteSampler : register(s0)` and `MaskSampler : register(s1)` and samples them in the **REVERSE** of their declaration order, which is what makes the two candidate orderings distinguishable at all. It is the **only** GL gate that does **not** bind every texture through `effect.Parameters[...]`: `SpriteSampler` is left to `SpriteBatch`, which forces the sprite onto unit 0 *after* `EffectPass.Apply()`. That is what makes the slot numbering observable in the picture, and it is why every other GL gate stayed green while this shipped broken — under parameter binding a first-use-numbered table is internally consistent and renders correctly. Rendered with a RED sprite and a GREEN mask, output `(sprite.r, mask.g, 0, 1)`: **yellow = correct**, **black = the #189 bug** (first-use order put `MaskSampler` on unit 0 where `SpriteBatch` overwrote it, and `SpriteSampler` on unit 1 which nothing bound), red = both read unit 0, green = both read the mask. Both channels flip together, so no outcome can be confused with a tolerance artefact. The gate asserts three things: the candidate renders yellow, **the `mgfxc` golden also renders yellow** (the control — without it, both builds being wrong the same way would pass), and the two are pixel-identical. It also prints the decoded sampler table for both builds, so a failure names which sampler landed on which unit. Measured RED before the fix (candidate `(0,0,0)`, maxd 255, 4096/4096 px) and **maxd 0** after. **Arm "sparse"** (`SamplerRegisterSparse.fx`) isolates the other variable, the ABSOLUTE register value: two samplers at `s2`/`s3` with nothing at `s0`/`s1`, sampled strictly IN declaration order so ordering cannot be what it measures. BLUE sprite + RED MaskA + GREEN MaskB, `(a.r, b.g, 0, 1)`: **yellow = registers honoured**, **green = compacted to units 0/1** (MaskA landed on unit 0 and SpriteBatch overwrote it) — only the red channel moves, and the untouched green is the control proving the harness bound anything at all. Measured `(0,255,0)` / maxd 255 / 4096 px RED before, **maxd 0** after, with the records going `ps_s0`/`ps_s1` at units 0/1 → `ps_s2`/`ps_s3` at units 2/3, structurally identical to the golden. The fixture must stay in LEGACY `sampler` syntax: `mgfxc` honours the annotation only there, and was measured to IGNORE it on the modern spelling. Unlike `SamplerPairsGl` arm A there is no golden-less mode: a missing golden is a hard failure. | `validation/SamplerRegisterOrderGl` (in-process compile vs the committed `mgfxc` `OpenGL` golden) | ✅ (`validation-render.yml`, ubuntu/llvmpipe) | `dotnet run -c Release --project validation/SamplerRegisterOrderGl` |
| **Real MonoGame OpenGL true 2-attachment MRT render** (Phase 51 A2, Phase 41 GAP-2's remaining rung) — `DeferredSprite.fx` (Nez's deferred renderer, the GAP-2 shader) drawn with **TWO render targets bound**, reading **BOTH attachments** back. This is the **only** gate in the repo that binds more than one render target, so it is the only thing that can distinguish "`COLOR1` reached attachment 1" from "the second output went nowhere" — a single-target gate cannot, and neither can a structural match, because the second output lives in the emitted GLSL (`gl_FragData[1]`) rather than in the `.mgfx` record tables. The sprite is drawn **1:1 texel-to-pixel** so no filter and no build's baked sampler state can shift a boundary; its left half is opaque and its right half sits below `_alphaCutoff`, so the shader's own `clip()` splits the surface in one draw. The normal map is a different colour AND a different alpha, put through the shader's arithmetic (`normal.a *= 0.5 * 0.5`) to land on a value (`alpha 50`) that neither the clear colour nor a copy of attachment 0 can produce. Both targets clear to **transparent** black, so "never written" is a nameable outcome rather than something that blends into a correct one. **Arm A** asserts the exact expected values on both attachments and names the failure mode (attachment 1 unwritten / attachment 1 == attachment 0 / slots swapped / `clip()` discarded only attachment 0 / `clip()` never fired), plus a non-vacuity count. **Arm B** pixel-diffs both attachments against the real `mgfxc` `OpenGL` golden: **maxd 0 on both**. Arm A is not redundant with arm B — the mutation check (binding one target instead of two) leaves the diff arm reporting maxd 0 while both sides are broken **identically**, and only arm A turns that red. Arm A is also run against the golden itself, so a diff failure is attributed to the right side rather than blamed on the candidate by default. | `validation/DeferredSpriteMrtGl` (in-process compare vs the real `mgfxc` `OpenGL` golden) | ✅ (`validation-render.yml`, ubuntu/llvmpipe) | `dotnet run -c Release --project validation/DeferredSpriteMrtGl` |
| **Real MonoGame OpenGL ShaderToy `.glsl`-route render** (Phase 51 A5) — the converter → pipeline route, end to end: `GradientToy.glsl` is converted **in process** by the real `ShaderToyConverter`, and ShadowDusk's OpenGL build of the result is pixel-diffed against **`mgfxc`'s build of the same converted `.fx`** on real MonoGame DesktopGL: **maxd 0**. §8 is right that there is no `mgfxc` oracle for ShaderToy *input*, but the converter's *output* is ordinary HLSL, so the **downstream half of the route can be held to the real product bar** — and that is precisely what this gate does; it makes no claim about fidelity to the original ShaderToy (that is §8's separate axis and its own oracle). **The golden is pinned to a specific `.fx`:** the driver asserts the converter still emits the committed `tests/fixtures/shaders/shadertoy/GradientToy.fx` **before** rendering, and writes the current output to the results dir on mismatch, so converter drift turns the gate red with a regeneration recipe instead of silently leaving the golden describing a different shader. Rendering uses a **real NDC fullscreen quad**, not `SpriteBatch`: the converted effect is VS-driven and its generated header states the host contract ("POSITION is already in NDC"), so `SpriteBatch`'s screen-space vertices would land outside the frustum and render nothing. The absolute arm asserts a genuine two-axis gradient (R varies across x, G across y, B==0, A==255) so a flat or black frame cannot pass as agreement, and it runs against the golden too. The fixture is deliberately time-independent (no `iTime`) so the comparison is deterministic. | `validation/ShaderToyRouteGl` (in-process convert + compare vs the real `mgfxc` `OpenGL` golden) | ✅ (`validation-render.yml`, ubuntu/llvmpipe) | `dotnet run -c Release --project validation/ShaderToyRouteGl` |
| **Real MonoGame DirectX ShaderToy `.glsl`-route render** (Phase 51 A5/A10) — the DirectX arm of the row above, and the same claim: `GradientToy.glsl` is converted **in process**, and ShadowDusk's `DirectX_11` build of the result is pixel-diffed against **`mgfxc`'s `DirectX_11` build of the same converted `.fx`** on real MonoGame `WindowsDX`. **It only became possible in Phase 51 A10.** Before that the converter emitted `vs_3_0`/`ps_3_0` in BOTH arms of its `#if OPENGL` header, and mgfxc refuses that for `/Profile:DirectX_11` (*"must be SM 4.0 level 9.1 or higher!"*), so no DirectX golden could exist — the route's own output was not buildable by the reference compiler on this target. The converter now emits an `SM4`-gated header whose DirectX arm names the `*_4_0_level_9_1` pair. Same fixture pinning as the GL arm (the driver asserts the converter still emits the committed `.fx` before rendering), same real-NDC-fullscreen-quad harness (not `SpriteBatch` — the effect is VS-driven and passes POSITION through untransformed), same absolute + vs-golden arms. **The golden is REQUIRED here**, unlike the GL arm's optional-golden shape: the mgfxc comparison is the entire point of this driver, so a missing golden fails rather than silently reporting an absolute-only pass. | `validation/ShaderToyRouteDx` (in-process convert + compare vs the real `mgfxc` `DirectX_11` golden) | manual (Windows + DX11 GPU) — **default-ON in `run-windows-render-gates.ps1`** (2026-07-31). No CI lane: there is no headless D3D driver a GitHub runner can use, the same bucket as every other DX gate. | `dotnet run -c Release --project validation/ShaderToyRouteDx`, or the gate script |
| **Real MonoGame DirectX** render vs `mgfxc`/`fxc` | `validation/VsDrivenDx`, `validation/BaselineDx` (mgfxc golden arm) + `validation/Candidate{Dx,Vkd3d}` + `compare_dx.py` | manual | `dotnet run --project validation/VsDrivenDx` for the VS rig; the CORPUS comparison needs all three: `dotnet run --project validation/BaselineDx`, then `… /CandidateDx`, then `python validation/compare_dx.py` (this is what `run-windows-render-gates.ps1` does) |
| **Real MonoGame DirectX Apos.Shapes render-proof** (Phase 51 A3, DX slice — Gum's SDF shape renderer). Same fixture the Vulkan proof uses, `apos-shapes-sm6.fx`: its DirectX macro set `{MGFX, HLSL, SM4}` takes the fixture's `#else` branch (`vs_4_0`/`ps_4_0`, legacy `sampler`/`tex2D` syntax — no separate DX fixture variant needed). The original A3 proof rendered one hand-built shape through a bespoke vertex-buffer harness and pixel-diffed BOTH ShadowDusk DXBC backends (`d3dcompiler_47` oracle and `vkd3d-shader`) against the real mgfxc `DirectX_11` golden: **maxd 0**, no tolerance needed. Phase 55 (2026-07-23) deleted that harness — `-- apos` now runs the full 30-cell `ShapeBatch` gallery instead (next row), which subsumes this proof. | `validation/VsDrivenDx -- apos` (in-process compare, `AposGalleryRenderer`) | manual (Windows DX11) — **default-ON in `run-windows-render-gates.ps1`** (2026-07-23) | `dotnet run -c Release --project validation/VsDrivenDx -- apos`, or the gate script |
| **Real MonoGame DirectX FULL SHAPE-GALLERY render-proof** (Phase 55, 2026-07-23). Same mode (`-- apos`), now the SAME 30-cell `ShapeBatch` gallery the GL/DX12/Vulkan gallery rows use, with two deliberate baselines (since 2026-07-23): `candidate-oracle` (`d3dcompiler_47`) diffs against the real, locally-generated `mgfxc` `DirectX_11` golden (`tests/fixtures/golden/DirectX_11/apos-shapes-sm6.mgfx`) at **tolerance 0 — maxd 0, all 30 cells**; `candidate-vkd3d` diffs against `Apos.Shapes`' own embedded, precompiled DX11 effect at **tolerance 0 — maxd 0** (disassembly shows that embedded effect is itself vkd3d-shader-compiled — a legitimate same-toolchain baseline, but NOT a valid oracle). An earlier "maxd 1 on 14/30 cells / missing `OptimizationLevel3`" finding was an artifact of diffing the `d3dcompiler_47` candidate against that vkd3d-compiled embedded effect (two independent compilers) and was resolved by the real-`mgfxc` oracle switch — see `validation/VsDrivenDx/Program.cs` remarks. | `validation/VsDrivenDx -- apos` (`AposGalleryRenderer`, four arms) | manual (Windows DX11) — **default-ON in `run-windows-render-gates.ps1`** (2026-07-23) | `dotnet run -c Release --project validation/VsDrivenDx -- apos`, or the gate script |
| **Real MonoGame DirectX12 (`WindowsDX12`) PS/SpriteBatch corpus** — ShadowDusk's own DX12 `.mgfx` (profile 2, SM6 DXIL, `DirectX12ShaderCodeWrapper`) vs a REAL `mgfxc` `DirectX_12` golden (built via `dotnet-mgcb` 3.8.5's own content pipeline, `/platform:WindowsDX12`, extracted from its XNB wrapper), real native `WindowsDX12` runtime ([Phase 54](../plan/DONE/PHASE-54-dx12-dxil-backend.md), 2026-07-23). **maxd 0, 10/10** — genuine oracle-backed proof, superseding the earlier own-output-only evidence. | `validation/BaselineDx12` + `CandidateDx12` + `compare_dx12.py` | manual (Windows + DX12 GPU) — **default-ON in `run-windows-render-gates.ps1`** (2026-07-23) | `dotnet run --project validation/BaselineDx12 -c Release`, then `.../CandidateDx12`, then `python validation/compare_dx12.py`, or the gate script |
| **Real MonoGame DirectX12 (`WindowsDX12`) VS-driven + Apos.Shapes** ([Phase 54](../plan/DONE/PHASE-54-dx12-dxil-backend.md), 2026-07-23). Renders both the same real `DirectX_12` goldens' `VsTransformColorTexture`/`apos-shapes-sm6` entries AND ShadowDusk's own compiled vertex shader on real `WindowsDX12` — **maxd 0 (apos), maxd 0 diffPixels (vs rig, tol 4)**. Previously crashed at draw time (native `E_INVALIDARG`/`0x80070057` in `MGG_GraphicsDevice_DrawIndexed`); root-caused by reading MonoGame's real, current v3.8.5 source directly to an empty vertex-attribute table (MonoGame's shared `VertexInputLayout.GenerateInputElements` needs it to build the D3D12 input layout) and fixed via `DxilVertexInputReflector`. | `validation/VsDrivenDx12` | manual (Windows + DX12 GPU) — **default-ON in `run-windows-render-gates.ps1`** (2026-07-23) | `dotnet run --project validation/VsDrivenDx12 -c Release` (mode `vs`) and `-- apos` (mode `apos`), or the gate script |
| **Real MonoGame DirectX12 FULL SHAPE-GALLERY render-proof** (Phase 55, 2026-07-23; root-caused 2026-07-31). The `-- apos` mode above renders the SAME 30-cell `ShapeBatch` gallery the GL/DX11/Vulkan gallery rows use, diffed against the real, locally-generated `mgfxc` `DirectX_12` golden (`tests/fixtures/golden/DirectX_12/apos-shapes-sm6.mgfx`) at **tolerance 1** — the package's embedded effect was dropped as a baseline on 2026-07-23 (its DX11 sibling disassembles as vkd3d-shader-compiled, from the same non-`mgfxc` build pipeline, so it is not a valid oracle). Result: **maxd 1 on 11 pixels of 402,984**, every one exactly ±1 in a single channel. **Root cause (2026-07-31): the pinned DXC build, not a ShadowDusk defect** — ours is `dxcoob 1.7.2212.40` (`Vortice.Dxc` 3.3.4), the golden's is `dxcoob 1.8.2505.32` (MonoGame 3.8.5's bundled DXC), both readable in the blobs' `!llvm.ident`. ShadowDusk's own HLSL + own flags through a DXC 1.8 build reproduce the golden's DXIL instruction-for-instruction and render at **maxd 0** (see the §7 row, and the driver's own remarks, for the full chain and the opt-in `SHADOWDUSK_DX12_PROBE_MGFX` arm that proves it). Phase 54's own VS-driven maxd-0 result against the same golden stands. | `validation/VsDrivenDx12 -- apos` (`AposGalleryRenderer`) | manual (Windows + DX12 GPU) — **default-ON in `run-windows-render-gates.ps1`** (2026-07-23) | `dotnet run --project validation/VsDrivenDx12 -c Release -- apos`, or the gate script |
| **Preprocessed-HLSL dump** (diagnostic, no GPU, no assertions — not a gate). Dumps the exact HLSL text `CompilationPipeline` hands to DXC for a given `.fx` + `PlatformTarget`, plus the `-D` macro flags. Exists so a DXC-side divergence can be attributed: replay the identical input through a different `dxc.exe` and diff `dxc -dumpbin` output. An empty *instruction* diff means our source and flags are right and only the DXC pin differs — the reasoning that closed the DX12 1/255 question. | `validation/DumpPreprocessedHlsl` | never (diagnostic, run on demand) | `dotnet run --project validation/DumpPreprocessedHlsl -- tests/fixtures/shaders/third-party/Apos.Shapes/apos-shapes-sm6.fx DirectX12 out.hlsl` |
| **DirectX modern features render** (vertex texture fetch; vkd3d vs `fxc`) | `validation/DxModernFeatures` | manual | `dotnet run --project validation/DxModernFeatures` |
| **Real FNA** render vs `fxc /T fx_2_0` | `validation/FnaValidation` | manual | `dotnet run --project validation/FnaValidation` |
| Forward-compat — **the full range of MonoGame releases that can load our v10 output**, not one anchor version. Swept 2026-07-28 (Phase 52 Area A) over **seven consecutive releases: `3.8.1.263` (floor) / `3.8.1.303` / `3.8.2.1105` / `3.8.3` / `3.8.4` / `3.8.4.1` / `3.8.5` stable** — **70 renders, the same v10 bytes, pixel-identical (maxd 0) on every runtime** and all within tolerance of the mgfxc goldens (`Scanlines`/`Dots` at 1/255, the rest 0). The **floor is measured, not assumed**: every stable DesktopGL release was probed, and `3.8.0.1641` is the one that rejects our output (`"This MGFX effect seems to be for a newer release of MonoGame"` — its loader predates MGFX v10), a real MonoGame boundary rather than a ShadowDusk gap. A runtime-integrity guard fails any cell whose loaded MonoGame version does not match the requested one. | `validation/ForwardCompat` + `compare_forwardcompat.py` | manual | `validation/ForwardCompat/run-forwardcompat.ps1` |
| **KNI WebGL** render (browser; 11-shader SD corpus = the 10 PS-only + the #107 do-while repro, Reach + HiDef) | `tests/ShadowDusk.BrowserTests` (Playwright) | manual / CI (`browser-smoke`) | see `tests/ShadowDusk.BrowserTests/README.md`; `node run-harness.mjs --corpus=sd` (Reach) / `--corpus=sd-hidef` (WebGL2) |
| **Real KNI OpenGL desktop** render vs mgfxc + MonoGame (KNI v4.02, SDL2.GL) | `validation/KniDesktopGL` + `compare_kni.py` | manual — **default-ON in `run-windows-render-gates.ps1`** (2026-07-19) | `dotnet run --project validation/KniDesktopGL` then `python validation/compare_kni.py`, or the gate script |
| **Real KNI DirectX** render vs mgfxc (KNI v4.02, WinForms.DX11; in-process compare) | `validation/KniWinFormsDX` | manual (Windows DX11) | `dotnet run --project validation/KniWinFormsDX -c Release` |
| **Real KNI OpenGL VS-driven** render vs mgfxc (issue #70: matrix transpose + legacy `: POSITION`, KNI v4.02 SDL2.GL) | `validation/KniVsDriven` (in-process compare) | manual — **default-ON in `run-windows-render-gates.ps1`** (2026-07-19) | `dotnet run --project validation/KniVsDriven`, or the gate script |
| **KNIFX v11** render in real KNI (vs v10) | `validation/KniDesktopGL knifx` + `compare_kni.py` | manual | `dotnet run --project validation/KniDesktopGL -- knifx` |
| **MGFX v11** render in real MonoGame 3.8.5 (vs v10 + goldens). Re-proven on **3.8.5 STABLE** 2026-07-28 (Phase 52 Area B, harness re-pinned off `3.8.5-preview.6`): result table unchanged cell for cell — 10/10 load + render, maxd 0 vs v10, <= 1 vs the mgfxc goldens. | `validation/MonoGameV11` + `compare_mgfxv11.py` | manual | `dotnet run --project validation/MonoGameV11` then `... -- v10`, then `python validation/compare_mgfxv11.py` |
| **Real MonoGame Vulkan (DesktopVK)** PS-corpus render (ShadowDusk's own Vulkan `.mgfx`, real container, profile 80, 10/10). Own-output proof only: the pixel-diff vs `mgfxc` is blocked FOR THIS CORPUS by the MonoGame `SlotOffset` bug (auto-numbered resources) — `compare_vulkan.py` auto-upgrades once fixed. Effects with **explicit registers** are not affected and DO get a real mgfxc pixel-diff; see the `validation/VsDrivenVulkan` row below. | `validation/CandidateVulkan` (+ `validation/BaselineVulkan`, `compare_vulkan.py`, `decode_mgfx_vulkan.py`) | manual (Windows + Vulkan GPU) — **default-ON in `run-windows-render-gates.ps1`** (2026-07-22, issue #145) | `dotnet run --project validation/CandidateVulkan`, or the gate script |
| **Real MonoGame Vulkan (DesktopVK) VS-DRIVEN render vs the `mgfxc` 3.8.5 golden** — a genuine reference-compiler oracle on Vulkan (issue #145). Renders a NON-IDENTITY asymmetric matrix through a custom vertex buffer and pixel-diffs both arms in-process; **maxd 0**. Also rejects a blank frame (the reported symptom was "renders nothing"). The fixture's Vulkan branch uses explicit registers, which keeps mgfxc's slot arithmetic in range so the baseline actually loads — the workaround that unblocks what Phase 32 could not do. Mutation-proven: restoring `-Zpr` turns it red at maxd 255. **Phase 2 renders the ISSUE #145 REPRODUCER ITSELF** — upstream `Apos.Shapes` (`apos-shapes-sm6.fx`). As originally built (a hand-rolled vertex-buffer harness at commit `ea38c6d8`) it hit **maxd 0** vs the mgfxc golden and was mutation-proven at maxd 255; since Phase 55 (2026-07-23) the mode renders the real package's full 30-cell `ShapeBatch` gallery instead — see the next row. Each phase is its own process: DesktopVK does not survive a second GraphicsDevice in one process. | `validation/VsDrivenVulkan` (in-process compare, two phases) | manual (Windows + Vulkan GPU) — **default-ON in `run-windows-render-gates.ps1`** | `dotnet run -c Release --project validation/VsDrivenVulkan` (phase 1) and `… -- apos` (phase 2), or the gate script |
| **Real MonoGame Vulkan FULL SHAPE-GALLERY render-proof** (Phase 55, 2026-07-23). Phase 2 (`-- apos`) above now renders the SAME 30-cell `ShapeBatch` gallery the GL/DX11/DX12 gallery rows use, golden arm = `Apos.Shapes`' own embedded Vulkan effect. **maxd 0 across all 30 cells** — no divergence at all, unlike the DX11/DX12 1-ULP findings. | `validation/VsDrivenVulkan -- apos` (`AposGalleryRenderer`, both arms) | manual (Windows + Vulkan GPU) — **default-ON in `run-windows-render-gates.ps1`** | `dotnet run -c Release --project validation/VsDrivenVulkan -- apos`, or the gate script |
| **Android on-device compile + `Effect` load probe** (Phase 50: an HLSL string compiled in memory on the device → `.mgfx` → live MonoGame `Effect`; verdict via clear colour + the `SHADOWDUSK` logcat tag; the on-device pixel-vs-`mgfxc` render diff is the remaining rung). **Pin moved to `MonoGame.Framework.Android` 3.8.5 on 2026-07-28 (Phase 52 F2) — BUILD-verified only; the on-device run recorded in Phase 50 was taken on 3.8.4.1 and has not been repeated on 3.8.5.** | `validation/AndroidGl` | manual (Android emulator / device + the Android workload) | `dotnet build validation/AndroidGl/AndroidGl.csproj -c Debug -t:Run` then `adb logcat -s SHADOWDUSK` |
| **ANGLE D3D11 derivative-shape probe** (issue #136: renders the emitted fragment control-flow shapes — top-level control, the PRE-fix one-shot for-loop, the POST-fix unwrapped form — on a real Windows browser's ANGLE Direct3D11 backend and asserts derivatives read live/dead as expected; the `RENDERER:` line must name `Direct3D11` for the run to count. Shape-level, not a full in-engine `.mgfx` load — see the §7 #136 row.) | `validation/AngleDerivativeProbe` (headless Edge, `--use-angle=d3d11`) | manual (Windows + Edge/Chromium) — **default-ON in `run-windows-render-gates.ps1`** (2026-07-19) | `./validation/AngleDerivativeProbe/run-angle-probe.ps1` (self-asserting), or the gate script |
| **MGCB content-processor plugin — a REAL `dotnet mgcb` content build through ShadowDusk** ([Phase 29](../plan/DONE/PHASE-29-mgcb-content-processor-plugin.md), 2026-07-31). **Not a render gate** — a *delivery-shape* gate. Writes a `.mgcb` that `/reference:`s the built `ShadowDusk.MgcbPlugin.dll` and selects `ShadowDuskEffectImporter`/`ShadowDuskEffectProcessor`, runs the pinned `dotnet-mgcb`, and per fixture × platform asserts four things: MGCB exits 0; the `.mgfx` payload inside the produced `.xnb` is **byte-for-byte the `ShadowDuskCLI` binary's** output for the matching `/Profile:`; the `.xnb` **envelope** (header, type-reader manifest, shared-resource count, type id) is byte-for-byte the one MGCB writes for its OWN stock `EffectProcessor`; and the payload **differs** from stock (the positive proof ShadowDusk, not MonoGame's built-in compiler, produced it). Covers `/platform:DesktopGL` → OpenGL and `/platform:Windows` → DirectX. The CLI arm is a separate process on purpose: comparing against another in-process `EffectCompiler` call would only compare the library to itself and would stay green if the plugin's MGCB-context→`CompilerOptions` mapping silently changed. **Measured green on `dotnet mgcb` 3.8.2.1105, 3.8.3, 3.8.4, 3.8.4.1 and 3.8.5** (identical payload on all five), and from a real packed `.nupkg` extracted to a bare directory. Not in CI: `dotnet test` has no `dotnet-mgcb`, and the runner does not `dotnet tool restore`. The in-suite half (processor driven in-process, same byte-identity bar vs the CLI binary) IS in CI as `MgcbPluginByteIdentityTests`. | `validation/MgcbPlugin` (self-asserting; also `tests/ShadowDusk.Integration.Tests` `MgcbPluginByteIdentityTests`) | ❌ driver (needs `dotnet-mgcb`) / ✅ the in-suite half — **default-ON in `run-windows-render-gates.ps1`** (2026-07-31) | `dotnet tool restore`, then `dotnet run -c Release --project validation/MgcbPlugin`, or the gate script |
| **Phase 45 B10 reserved-word free uniform (`float noise;`) binds by name + drives output through MGFX v11 / KNI MGFX v10 / KNIFX** (in-process assert; same non-vacuous bar as `ReservedWordGl`: `noise=0.25 -> grey 64`, `0.75 -> 191`, differentiation 127). Re-proves B10 in the other containers the shared GL container path writes (v10 GL alone is `ReservedWordGl` above). Shared probe `validation/Shared/ReservedWord/ReservedWordProbe.cs`. | `validation/MonoGameV11 -- reservedword` (MGFX v11, real MonoGame 3.8.5); `validation/KniDesktopGL -- reservedword` (MGFX v10, real KNI v4.02); `validation/KniDesktopGL -- reservedword knifx` (KNIFX v11, real KNI v4.02) | manual | `dotnet run --project validation/MonoGameV11 -- reservedword`; `dotnet run --project validation/KniDesktopGL -- reservedword`; `dotnet run --project validation/KniDesktopGL -- reservedword knifx` |

### 6.1 The reference-compiler (oracle) pin — do not let it float

Almost every row above is "vs the `mgfxc` golden", so **which `mgfxc` built the goldens is part of the
evidence.** Settled on measurement 2026-07-28 ([Phase 52](../plan/DONE/PHASE-52-monogame-3.8.5-support.md)
Area C): the committed goldens are canonical **MGFX v10**, and `tools/compile-fixtures.ps1` now resolves
mgfxc from the **`dotnet-mgcb` version pinned in `.config/dotnet-tools.json`** (3.8.4.1) and **asserts the
container version byte of everything it writes**, refusing to overwrite the corpus with a different one.

It used to take "the highest `mgfxc.exe` in the NuGet cache", which was wrong twice over:

- **mgfxc 3.8.5 emits MGFX v11**, not v10 (verified: version byte 11, every file 58-2232 bytes larger,
  **0 of 46 matched its golden** on either profile). Correct for a 3.8.5 consumer, useless as the v10 oracle.
- The **`dotnet-mgcb-editor-windows` 3.8.4.1** package ships `mgfxc.exe` **without
  `SharpDX.D3DCompiler.dll`**, so it throws on every shader — and that was exactly the binary the old probe
  selected, meaning a golden regeneration would have compiled **0/46** and reported them all as failures.

Verified equivalence behind the pin: mgfxc **3.8.2.1105** and **3.8.4.1** (from the intact `dotnet-mgcb`
payload) each reproduce **all 46 committed goldens byte-for-byte on both OpenGL and DirectX_11**, so the pin
is a floor-to-3.8.4.1 range, not a single magic build. `validation/ReservedWordGl` resolves its bonus mgfxc
arm through the same pin for the same reason. **Moving this pin means regenerating goldens deliberately,
with a recorded corpus diff.**

**The "test programmatically" goal:** the path to a fully self-checking matrix is (a) promote the
`validation/*` render gates into CI jobs (where a software/headless driver exists), and (b) back this matrix
with a machine-readable coverage manifest a test asserts against (so a cell cannot be marked ✅ without a
passing test). **(a) is now partly done (Phase 44 C):** the **seven** in-process, self-asserting **GL** render
gates (`StateFidelity`, `CbufferModel`, `TextureBreadthValidation`, `ReservedWordGl`,
`SamplerPairsGl`, `DeferredSpriteMrtGl`, `ShaderToyRouteGl`) run in CI on ubuntu under
xvfb + Mesa llvmpipe via **`validation-render.yml`** (push-to-main + the `run-validation-render` PR label),
so a GL render regression turns the lane red. The **DX / FNA / KNI-DX** render gates stay manual — they
need a Windows runner with a software D3D driver (WARP), which is unverified, so they are deliberately not
wired yet (the remaining Phase 44 C tail).

## 7. Gaps & next targets (ordered)

| Gap | Achievable here? | Notes |
|---|---|---|
| **DirectX modern features render** | **done** | **VTF ✅** (`validation/DxModernFeatures`, vkd3d == `fxc` maxd 0). Texture-array render is **N/A from our side** (closed): our DXBC is correct + pinned, but MonoGame has no public `Texture2DArray` binding API to render through — a MonoGame limitation, not a ShadowDusk gap. |
| **KNI v4.02 render** (desktop `SDL2.GL` + DirectX + WebGL) | **done** | ✅ **Desktop SDL2.GL** (`validation/KniDesktopGL`, 2026-06-14): v10 maxd 0 vs MonoGame, ≤1 vs mgfxc goldens, 10/10. ✅ **DirectX WinForms.DX11** (`validation/KniWinFormsDX`, 2026-06-15): ShadowDusk DX vs mgfxc DX golden, maxd ≤1, 10/10. ✅ **WebGL Reach + HiDef** (`tests/ShadowDusk.BrowserTests`, refreshed 2026-06-15 on KNI v4.2.9001.2; corpus extended 2026-06-27): 11/11 of ShadowDusk's own `.mgfx` (the 10 PS-only + the **#107 do-while repro**) load + render in real KNI WebGL, issue-#7 HiDef guard GREEN, **#107 `maxDelta=0` on both profiles**. All three KNI render paths now proven on the current v4.02 line. The desktop rig is also Phase 35 Area B's reproduce-first baseline for the KNIFX writer. |
| **Promote `validation/*` render gates into CI** | partly **done** | **Seven GL in-process gates wired into CI** (`validation-render.yml`, ubuntu/llvmpipe, push-to-main + label): `StateFidelity`, `CbufferModel`, `TextureBreadthValidation`, `ReservedWordGl`, `SamplerPairsGl`, `DeferredSpriteMrtGl`, `ShaderToyRouteGl`. The `ShadowDusk.ImageTests` GL render already runs in CI. **DX / FNA / KNI-DX** render gates remain manual — they need a Windows runner + a software D3D driver (WARP), unverified. |
| **Machine-readable coverage** backing this matrix | **compile rung done** | `ValidationMatrixCoverageTests` pins the compile/reject cells as a `[Theory]`. Extending it to assert the render cells against the `validation/*` gates is the remaining step. |
| **MGFX v11 / KNIFX writers** | **committed, in progress** | Additive outputs we **will** emit so consumers can *use* the new-container features (KNIFX's XNA-compat/quality fixes; MonoGame v11's body), not a "won't do." v10 staying forward-compatible is a convenience, **not** a reason to skip these. Default stays v10 for universal load; v11/KNIFX are opt-in / auto-selected from the target, never required (seamless rule preserved). Path: reproduce-first render against KNI v4.02 (Phase 44 D) -> build the faithful writers (Phase 35 Area B). See [`DONE/PHASE-35-appendix/`](../plan/DONE/PHASE-35-appendix/). |
| **Vulkan render** | **done (2026-07-18, Phase 32)** | MonoGame 3.8.5 shipped `DesktopVK` stable; ShadowDusk's own Vulkan `.mgfx` (real container, profile 80) render-proven 10/10 on real DesktopVK, visually confirmed. **A pixel-diff vs `mgfxc`'s own Vulkan output now exists for explicit-register effects** — `validation/VsDrivenVulkan` (issue #145, 2026-07-22) matches the mgfxc 3.8.5 golden at maxd 0 on both the VS-driven transform and the `Apos.Shapes` reproducer. It stays blocked **only for auto-numbered resources** (this 10-shader PS corpus): mgfxc's output crashes on load there, a confirmed independent MonoGame bug (`SlotOffset` arithmetic) — an external dependency on MonoGame fixing it, not a ShadowDusk gap. See `plan/DONE/PHASE-32-appendix/vulkan-mgfx-format-spec.md`. |
| **DX12 target + render** | **done (2026-07-23, [Phase 54](../plan/DONE/PHASE-54-dx12-dxil-backend.md))** | `PlatformTarget.DirectX12` (profile 2, SM6 DXIL) is fully rung-4 proven: real `mgfxc` `DirectX_12` golden, maxd 0, for both the PS/SpriteBatch corpus and VS-driven/Apos.Shapes on real MonoGame 3.8.5 `WindowsDX12`. See the MonoGame/DirectX12 cell in §1. |
| **DX12 Apos.Shapes gallery `maxd 1` (11 pixels)** | **root-caused 2026-07-31 — the DXC pin, NOT a ShadowDusk defect. Closing it means a DXC bump, so it stays open as a scoped follow-up, not a bug.** | `validation/VsDrivenDx12 -- apos` sits at maxd 1 on 11 pixels of 402,984 against the real `mgfxc` `DirectX_12` golden. Both sides compile the same HLSL to DXIL with **different DXC binaries** — ours `dxcoob 1.7.2212.40 (e043f4a12)` (`Vortice.Dxc` 3.3.4's pin), the golden's `dxcoob 1.8.2505.32 (b106a961d)` (MonoGame 3.8.5's bundled DXC) — each readable in its own blob's `!llvm.ident`. **Proof it is nothing else:** (1) feed ShadowDusk's own pre-parsed HLSL (`validation/DumpPreprocessedHlsl`) and own DXC flags to a DXC 1.8 build and the golden's pixel-shader DXIL comes back **instruction-for-instruction identical** — the `dxc -dumpbin` diff is three lines (shader hash, `!llvm.ident`, `!dx.valver`) and zero instructions; add `-Qstrip_reflect` and the container matches at the same 41876 bytes with the same six parts at the same offsets; (2) swap **only** that DXIL payload into ShadowDusk's own `.mgfx` and render it through the same gate (`SHADOWDUSK_DX12_PROBE_MGFX`): **maxd 0, zero differing pixels**. The two DXC builds emit identical DXIL *intrinsic* counts and differ only in `fast`-math-licensed rewrites (1.7 if-converts more: 87 fewer branches, 34 more selects; commutative `fmul` reordering; eight `x - y*c` → `x + y*(-c)` folds). It reaches a pixel at all only because the shader adds ±half an 8-bit LSB of dither immediately before quantization, so a sub-ULP difference flips exactly the pixels on the rounding boundary. **What would reclassify this as a defect:** a *non-comment, non-`!llvm.ident`* line in that step-(1) disassembly diff, i.e. evidence our source or flags differ from `mgfxc`'s. As long as it is empty, the pin is the only variable. **Cost of closing:** bumping the DXC pin is cross-target (the same DXC feeds OpenGL/Vulkan SPIR-V and DX11 reflection; `Vortice.*` is capped *by* this pin; our macOS/Android/WASM natives are built from the same commit), so it re-baselines every golden and re-runs rung 4 everywhere — a deliberate change, never routine. See `plan/DONE/PHASE-55-apos-shapes-shape-gallery-render-proof.md` §8. |
| **DX12 DXIL carries a `STAT` part the reference strips** | **open — cosmetic, not scoped** | The real `mgfxc` `DirectX_12` golden's DXIL container has six parts; ShadowDusk's has seven, the extra one being `STAT` (~2.5 KB per shader) plus the HLSL identifier names (`$Globals`, `TextureSampler`, `BlueNoiseTex`, …) left in the DXIL metadata. `mgfxc` strips reflection; we do not. Phase 54's note that the "spurious `RTS0`/`STAT` parts" were removed is half-right — `RTS0` went, `STAT` never did. **Behaviorally inert**, measured: the DXC-1.8 probe arm above rendered maxd 0 *with* `STAT` present. Closing it is one `-Qstrip_reflect` in `DxcFlagBuilder`'s `DirectX12` case, but it moves DX12 output bytes, so it needs its own DX12 render re-proof and was deliberately not done as a side effect of a diagnosis (2026-07-31). |
| **DX12 DXIL signing off-Windows (`SD0214`)** | **open — external (Microsoft `dxil.dll`)** | DXC's DXIL validation/signing runs through `dxil.dll`, which is **Windows-only** (a no-op on Linux; absent entirely on macOS). A `DirectX12` compile on a non-Windows host therefore emits **unsigned** DXIL: it loads only with Windows Developer Mode enabled and **retail D3D12 rejects it at pipeline-state creation**. Surfaced since 0.15.0 as the compile-time **`SD0214`** warning (`DxcShaderCompiler`) rather than a silent per-host output divergence. **Consequence for reach:** DX12 is the one target whose output is *not* host-independent, and it is excluded from the cross-host byte-identity manifest (which covers `DirectX_Vkd3d` / `FNA` / `OpenGL`). **Workaround:** build DX12 content on Windows. Closing this needs either a redistributable cross-platform signer or DXC dropping the signing requirement, neither in ShadowDusk's control. |
| **OpenGL sampler records per (texture, sampler) PAIR** | **done (2026-07-29, [Phase 51](../plan/PHASE-51-consolidated-remainder-backlog.md) A7)** | The GL sampler table is keyed on the **(texture, sampler) pairs** SPIRV-Cross folds into combined samplers, in **its** declaration order, derived from the SPIR-V by the pure-managed `SpirvCombinedSamplerPairs` (so the CLI and browser hosts produce identical bytes). This closed two defects: (a) several textures read through **one shared `SamplerState`** (the classic diffuse+lightmap shape, ordinary HLSL that `mgfxc` compiles — its golden `tests/fixtures/golden/OpenGL/PenumbraTexture.mgfx` carries **two** records) was rejected outright with `SD0216`; and (b) a **silent** sibling `SD0216` could never see — SPIRV-Cross declares combined samplers in **first-use** order, so sampling out of declaration order left the counts matching while both the texture parameter and the sampler-type byte came out **swapped** (probed with a `Texture2D`+`TextureCube` pair: the GLSL declared `ps_s0` as `samplerCube` while the record claimed 2D). `SD0215` and `SD0216` are both **retired** (bind slots are no longer consulted for GL naming, so `register(s3)` compiles); `SD0217` covers the shapes the ordering model does not cover plus an internal cross-check against the sampler uniforms the emitted GLSL declares. **Rung 4:** `validation/SamplerPairsGl` on real MonoGame DesktopGL — arm A renders a 3-way-discriminating asymmetric scene proving each pair bound its own texture, arm B proves each record carried its own baked sampler state in declaration order. **Also fixed here: DirectX 12** was NOT in the texture-keyed branch and had defect (a) silently (one record, `Lightmap` never bound); DX11 was always correct. Two deliberate non-changes are recorded in `project_facts.md`: our texture-parameter naming (`DiffuseMap` vs `mgfxc`'s `TextureSampler+DiffuseMap`) and DX12's record-name field. |
| **MGCB Tier-1 "`mgfxc` on `PATH`" override** | **CLOSED (2026-07-31) by the content-processor plugin — the `PATH` mechanism itself still does not fire and never will** (measured 2026-07-28, [Phase 52](../plan/DONE/PHASE-52-monogame-3.8.5-support.md) Area E) | Area E set out to confirm the Tier-1 drop-in still worked on 3.8.5 and found it does not work on **any** supported version. Measured directly on Windows with a real `mgfxc.exe` shim first on `PATH` that logs every invocation and then delegates to the pinned mgfxc: building a `.mgcb` with `dotnet mgcb` **3.8.2.1105, 3.8.4.1, and 3.8.5** produced a valid `.xnb` every time with **zero shim invocations**. Corroborated statically: no `mgcb`/`MonoGame.Framework.Content.Pipeline` assembly in any of the three payloads contains the string `mgfxc` or `MGFXC_WINE_PATH` (the only hits are inside the standalone `mgfxc.dll` tool itself), and `MonoGame.Content.Builder.Task`'s `.targets` only ever execs `mgcb`. **MGCB compiles `.fx` in-process** (SharpDX `D3DCompiler` through 3.8.4.1; bundled DXC + MojoShader native tool packages at 3.8.5) and never shells out to an external effect compiler. **Measured on Windows only** — the Linux/macOS half is inferred from the identical payloads, not run. **Resolution:** [Phase 29](../plan/DONE/PHASE-29-mgcb-content-processor-plugin.md) shipped the in-process content-processor plugin on **2026-07-31**, and that is the native MGCB integration path. The gap is closed by the plugin, never by making the `PATH` route work — it cannot be made to work. See the plugin's own §6 row for the evidence. Compiling `.fx` with the ShadowDusk CLI directly, or calling `EffectCompiler` at runtime, are unaffected and remain supported routes. |
| **mgfxc's per-target compile-profile floor (reject fidelity)** | **DirectX done (2026-07-31, [Phase 51](../plan/PHASE-51-consolidated-remainder-backlog.md) A10); OpenGL + Vulkan open; DX12 unmeasured** | A recognized profile can still be one the target's reference compiler refuses. **DirectX is closed:** `SD0015` now rejects anything outside `{vs,ps}_{4_0_level_9_1, 4_0_level_9_3, 4_0, 4_1, 5_0}`, the set measured against the pinned mgfxc — which notably excludes `_level_9_0` **and every SM6 profile**, so a `major >= 4` rule would be wrong in both directions. It turned 20 corpus fixtures from silent ACCEPT into a loud reject on `DirectX_11`, every one of which real mgfxc already refused (14 vendored Nez shaders, `FnaMultiPassStates.fx`, `examples/Ex{Int,Mat3}UniformMember.fx`, and the 3 new A10 fixtures); no fixture's output bytes moved. **Still open:** the OpenGL SM ≤ 3 ceiling and the Vulkan `*_6_0`-only floor — same class, own blast radius each (the OpenGL one alone would newly reject 7 root fixtures). **DirectX 12's floor was deliberately not guessed**: its reference compiler is mgfxc 3.8.5, outside the golden oracle pin. Full measurement table: §8.1. |
| **Metal target** | ⬛ | ShadowDusk Metal is a stub; FNA's Metal backend is already covered by the one `.fxb`. |
| **Issue [#107](https://github.com/kaltinril/ShadowDusk/issues/107) — `do{}while(false)` rejected by WebGL** | **CLOSED 2026-06-27 (now WebGL render-proven, both profiles)** | A helper with a nested `if` + early `return` (Vic's `TestEarlyReturn`) made SPIRV-Cross emit a one-shot `do { … break; … } while(false);` in the GL GLSL (the early `return` becomes `break`). The effect compiled + loaded on desktop GL but **WebGL/KNI Reach rejected the `do`-loop at load** (GLSL ES 1.00 Appendix A doesn't guarantee do-while). **Fixed** by `MonoGameGlslRewriter` **Rule 9** (the sibling of the Rule 8 `roundEven` lowering): each one-shot do-while is lowered to the WebGL1-safe `for (int _i = 0; _i < 1; _i++) { … }` — semantically identical (one iteration, `break`/`continue`/fall-through preserved → pixels unchanged) and the Appendix-A-allowed form (which `mgfxc`/MojoShader effectively use too). Proven do-while-free end-to-end on all 4 affected corpus shaders (ClipShader, ClipShaderSpriteTarget, apos-shapes, the #107 repro); byte-identity manifest regenerated for the 2 affected GL entries (DX/FNA bytes unchanged); rewriter unit + `HidefGeneralityFixtureTests` pipeline tests pin it; full suite green. **Remaining rung now CLOSED (2026-06-27):** the `#107` repro (`examples/Issue107DoWhile.fx`) is wired into the KNI WebGL browser harness (`tests/ShadowDusk.BrowserTests`, `--corpus=sd` + `--corpus=sd-hidef`) and **loads + renders `maxDelta=0` in real KNI WebGL on BOTH profiles** — Reach (WebGL1 / GLSL ES 1.00, the exact context that rejected the `do`-loop) and HiDef (WebGL2 / GLSL ES 3.00). ShadowDusk's OWN bytes, vs the desktop-GL render of the same bytes; the do-while-free GLSL is confirmed in the emitted `.mgfx`. CI auto-covers it (the `browser-smoke` job runs the same harness). Distinct from #106 (CLOSED, Phase 45 parser fix). **Superseded in part by #136 (2026-07-19):** the for-loop lowering is now the FALLBACK (Rule 9b) — the entry-point wrapper is UNWRAPPED into straight-line `main` with real early returns (Rule 9a), because the one-shot for-loop, while load-safe, silently zeroes gradient ops on ANGLE D3D11 (see the #136 row). The WebGL load-safety this row proved is preserved (early `return` in `main` is valid ESSL 1.00; the #107 repro + browser harness stay green). |
| **Issue [#136](https://github.com/kaltinril/ShadowDusk/issues/136) — `dFdx`/`dFdy` return 0 in Windows browsers (ANGLE D3D11 gradient poisoning)** | **fix landed 2026-07-19; ANGLE-D3D11 render proof = remaining rung** | Reported by Apostolique with a 7-variant WebGL probe matrix: on ANGLE's D3D11 backend (WebGL in every Chromium/Edge/Firefox on Windows), ANY gradient op inside a loop with a divergent exit — a conditional `break` OR `discard` — evaluates to `0.0`, with `COMPILE_STATUS`/`LINK_STATUS` both true (FXC X4014's rule, silently "recovered" by ANGLE). The Rule-9 for-loop lowering of SPIRV-Cross's entry wrapper put the ENTIRE fragment body in such a loop, so every derivative died — Apos.Shapes' SDF antialiasing visibly off in-browser vs the MojoShader baseline. His probe also proved conditional `break` ALONE poisons (a discard-only fix is insufficient) and that straight-line `main` + conditional discard/early-return keeps derivatives alive — the shape the rewriter now emits (Rule 9a unwrap; `docs/glsl-uniform-naming.md` rules 9a/9b). The fix's own adversarial review then found and closed two more pieces in the same pass: 9a recurses through the plain blocks it creates, so an **inlined helper that both early-returns and takes a derivative** (its one-shot wrapper nests inside the entry wrapper) unwraps too instead of falling back to a poisoned for-loop; and a tail whose duplication would move a gradient op / implicit-LOD sample into divergent flow is never unwrapped (undefined per GLSL §8.13.1 — those keep the 9b form, which leaves the op convergent after the loop, matching fxc). **Pinned:** rewriter unit tests (unwrap + every fallback), `EarlyReturnHelper…Issues107And136` + `EarlyReturnHelperGradient…Issue136` (end-to-end, entry + nested-helper shapes), and the vendored derivative-AA corpus revision `apos-shapes-aa.fx` with `AposShapesAa_OpenGl_NoGradientOpInsideDivergentLoop_Issue136` — a lexical analyzer that FAILS if any gradient op ever again lands inside a divergent loop in emitted GL GLSL. **ANGLE render proof (shape-level): CLOSED 2026-07-19** — `validation/AngleDerivativeProbe` (headless Edge, `--use-angle=d3d11`) renders the emitted control-flow shapes on real ANGLE Direct3D11 and confirms: top-level control `red=255`, the PRE-fix one-shot for-loop shape `red=0` (bug reproduced), the POST-fix unwrapped shape `red=255` (fix proven), under `ANGLE (NVIDIA RTX 3080 Direct3D11 vs_5_0 ps_5_0)`. **Remaining rung (in-engine):** loading ShadowDusk's actual `.mgfx`/`.knifx` through KNI-in-a-Windows-browser (CI's ubuntu browser-smoke uses SwiftShader — structurally blind to this class, same "Windows box is the gate" bucket as DX/FNA). Apostolique offered his headless-Edge CDP harness (hooks `shaderSource`, reads back pixels); adopting it as the full Windows-only gate is the tracked follow-up. |
| **Phase 41 GAP-2 — DeferredSprite MRT `: COLOR0/1` output on GL** | **done — rung 4 (2026-07-29, [Phase 51](../plan/PHASE-51-consolidated-remainder-backlog.md) A2)** | The multi-render-target PS returns a struct with `: COLOR0/1` outputs; DXC's GL SPIR-V path rejected `COLOR` as a PS output (vkd3d/DX accepts it). **Fixed** by a **GL-only**, struct/entry-aware rewrite (`GlStructOutputColorRewriter`): it retargets only the PS-RETURN struct's members to `: SV_Target<n>` for the OpenGL DXC compiles (DX/vkd3d output **byte-identical**, verified by md5), never the PS-input interpolant `Color : COLOR0`. Plus a render-correctness fix in `MonoGameGlslRewriter`: true MRT (2+ outputs) now emits `gl_FragData[0]` for slot 0 (not `gl_FragColor`, which broadcasts to all attachments) — matching the mgfxc golden's `#define ps_oC0 gl_FragData[0]`/`[1]`. Pinned by `GlStructOutputColorRewriterTests` (unit) + `HidefGeneralityFixtureTests.DeferredSprite_Mrt_*` (end-to-end). **The render rung is now closed:** `validation/DeferredSpriteMrtGl` binds TWO render targets on real MonoGame DesktopGL, draws DeferredSprite, reads **both** attachments back, and pixel-diffs each against the real `mgfxc` `OpenGL` golden — **maxd 0 on both attachments**, plus an absolute arm asserting the exact values the HLSL implies (including that `clip()` discards both attachments, not just slot 0). Wired into `validation-render.yml`, so it runs in CI on Mesa llvmpipe. Mutation-proven: binding one target instead of two turns it red with the failure named. See the §6 row for what each arm discriminates and why the absolute arm is not redundant with the diff. **KNI WebGL still cannot host this rung** (its Blazor backend exposes no public multi-render-target binding API to a consumer, the same shape as the MonoGame `Texture2DArray` limitation above), so MRT stays a **desktop-only** proof — a platform limit, not an open gap. |
| **Phase 41 GAP-1 — macro-defined techniques on GL** | **open (GL only; DX + FNA done) — faithfulness-blocked, not just hard** | DX (existing fallback) and **FNA (2026-06-27)** recover macro-defined `TECHNIQUE()` techniques. **GL stays gated**: its macro set lacks SM4/SM6, so the stock effects expand to a legacy DX9/SM2 branch that crashes DXC's SPIR-V codegen (uncatchable AV) — kept as a loud `SD0010` rather than a crash. **Why the obvious shortcut is wrong (analysis 2026-06-27):** the tempting fix — add SM4 to the GL macro recovery so the effect takes the modern branch DXC can compile — would **violate faithfulness**. `mgfxc`'s own OpenGL target also defines only `{MGFX, GLSL, OPENGL}` (no SM4; mirrored in `PlatformMacros.For(OpenGL)`), so `mgfxc` compiles these stock effects from the **legacy SM2 branch** (via `fxc` + MojoShader). Forcing ShadowDusk onto the modern branch would produce a *different* effect than `mgfxc` emits on GL — a silent divergence from the "identical to `mgfxc`" promise. A genuinely faithful GL fix would need DXC to not crash on the legacy SM2 HLSL (or a managed legacy→modern transcription that is provably behavior-preserving), neither of which is a small change. Pinned by `GumFnaSampleShader_MacroTechnique_OpenGl_KeepsSd0010_GlMacroModelGap`. Not a blocker for Vic: these effects compile on **DX and FNA** (the backends Gum targets), and macro-technique recovery is proven there. |
| **Android — compile `.fx` on-device** | 🟢 **PROVEN on a real emulator (Phase 50, 2026-06-28); productionization follow-ups open** | **On-device, in-memory, runtime compile works**: a `pixel_7` API-34 emulator ran `validation/AndroidGl` — plain `new EffectCompiler().CompileAsync(hlslText, OpenGL)` compiled an HLSL string → 410-byte `.mgfx` → live MonoGame `Effect`, all on the device (green-screen). **DXC was cross-compiled for Android** (`arm64-v8a` + `x86_64`) by porting the `.wasm-build` recipe to the NDK (`.wasm-build/build-dxc-android.ps1`; the one new fix is `LLVM_INFERRED_HOST_TRIPLE` to bypass `config.guess`); SPIRV-Cross is a stock NDK CMake build. Three fixes took it to green: the APK-`lib/<abi>/` bare-SONAME loader branches; `ExcludeAssets="native"` to drop the desktop NuGets' wrong-platform linux `.so`; and `EffectCompiler` auto-selecting the managed `SpirvReflector` on Android (the default DXIL-oracle reflection throws there) — byte-identical, so `new EffectCompiler()` stays seamless. Desktop unchanged; full `dotnet test` green (1951). The `android-arm64` natives are now **hosted on the `native-dxc-1.7.2212.40` tag, SHA-pinned in `tools/restore.*`, and packed into `ShadowDusk.HLSL` / `ShadowDusk.GLSL` under `runtimes/android-arm64/native/` (shipping in 0.11.0)** — so a NuGet consumer is self-contained and the release / `pack-consume` CI hard-gates their presence. **Remaining (not blockers):** author `dxc-android-build.yml` to rebuild the natives in CI, host the `x86_64` emulator natives (a local-only dev convenience today), and an on-device **pixel-vs-`mgfxc` render diff** (today proves compile + `Effect` load). Build-time precompile also works. Tracked: [`plan/PHASE-50-android-runtime-support.md`](../plan/PHASE-50-android-runtime-support.md) §6.2. |
| **KNIFX container on KNI WebGL** | **CLOSED 2026-06-27 (render-proven in real KNI WebGL)** | ShadowDusk's `--target-runtime kni-knifx` used to emit a KNIFX whose backend directory carried only the **OpenGL** backend (`0x0011`); KNI **WebGL** reports a distinct backend (`KnifxBackend.WebGL = 0x0014`), found no match, and **rejected it** (*"Effect profile 'DirectX_11' is not compatible with the graphics backend 'WebGL'"*). **Fixed** by making `KnifxWriter` emit, for a GL target, a **multi-backend directory advertising the whole GL family** (OpenGL + GLES + WebGL) so one `.knifx` loads on every KNI GL host: the **OpenGL (desktop)** entry keeps the faithful version-directory body (byte-identical, desktop render unchanged), while the **GLES + WebGL** entries share a body whose per-shader `ShaderVersion` is **(0,0)** + raw GLSL — which routes KNI's **runtime** GL-ES converter, the EXACT mechanism that loads MGFX v10 on KNI WebGL (proven, Phase 33). **Render-proven both hosts:** KNI **desktop** SDL2.GL still 10/10 (`validation/KniDesktopGL knifx`); KNI **WebGL** now loads + renders the KNIFX container (`tests/ShadowDusk.BrowserTests` `--corpus=sd`: `ExKnifxMacro` __KNIFX__ red branch, 196096 red px / 0 green, Mode-1 12/12). Pinned by `KnifxWriterTests` (multi-backend dir + raw web body, 20/20) + the WebGL harness probe. Full suite 1940 green; DX/FNA/KNI-DX render gates unaffected (KNIFX-isolated change). |
| **Issues [#137](https://github.com/kaltinril/ShadowDusk/issues/137) / [#139](https://github.com/kaltinril/ShadowDusk/issues/139) / [#140](https://github.com/kaltinril/ShadowDusk/issues/140) — VS body lowerings, derivatives `#extension` header, nested `round()`** | **fixed 2026-07-22 (Phase 53)** | Three silent-Effect-load-failure classes from the issue-#136 adversarial sweep, all "compile exit 0, dies on a strict GL driver at runtime" (the field-report class behind "an error on the SpriteBatch call"): **#137** — the vertex stage returned before Rules 8/9b/10/11 ran, so a VS `round()` shipped `roundEven()` (rejected by Mesa strict + WebGL1) and a VS early-return helper shipped the raw `do{}while(false)` (Appendix A); Rules 8/9b/10/11 now run on the VS before `posFixup` injection, with **Rule 9a deliberately pixel-only** (its early `return;` would skip the posFixup tail). **#139** — derivative-using fragment shaders now ship `#extension GL_OES_standard_derivatives : enable` as the first line (mgfxc's exact behavior; `fwidth` included, which SPIRV-Cross emits directly). **#140** — Rule 8 resumes INSIDE its replacement so a `round()` nested in another's argument lowers too. Pinned by rewriter unit tests + the `examples/Issue137VsRound` / `Issue137VsEarlyReturn` / `Issue139DerivativeExtension` / `Issue140NestedRound` fixtures (`Phase53ErrorVisibilityRegressionTests`, GL structural + DX-still-compiles). Unchanged shapes proven byte-identical (cross-host byte-identity manifest green without regeneration). |
| **GL portability lint SD0400–SD0403 (issues [#141](https://github.com/kaltinril/ShadowDusk/issues/141) + [#138](https://github.com/kaltinril/ShadowDusk/issues/138) diagnostic half, [#149](https://github.com/kaltinril/ShadowDusk/issues/149)/[#163](https://github.com/kaltinril/ShadowDusk/issues/163) version-gate half)** | **lint landed 2026-07-22 (Phase 53); #138 both known shapes FIXED where provable, 2026-07-24; SD0403 added 0.15.0** | `GlslPortabilityAnalyzer` runs over every emitted MonoGame-GL stage and surfaces compile-time **warnings** (never rejections) through the new `CompiledShader.Warnings` channel / CLI stderr / `ValidateAsync`: **SD0400** (#141) a USER-written gradient op inside a divergent loop — 0.0 on ANGLE D3D11 where fxc warns X3553; **SD0401** a PS-only pass reading interpolants SpriteBatch's SpriteEffect VS never writes (beyond COLOR0/TEXCOORD0) — a strict-driver link failure at first draw; **SD0402** (#138) non-Appendix-A loop shapes (`for (;;)`, empty-increment `for`, `while`, genuine `do-while`) that may fail to load on WebGL1/KNI Reach. Pinned by `GlslPortabilityAnalyzerTests` + the `Sd0400GradientInDivergentLoop` / `Sd0401SpriteBatchInterpolant` fixtures (warn + still compile, GL-only). **#138 emission-side canonicalization, both shapes now fixed where the fix is provably safe:** Rule 12 hoists a constant-bounded loop's body-advanced index into the for-header (e.g. `GaussianBlur.fx`'s empty-increment form); Rule 13 gives a header-less `for (;;)` a real Appendix-A-legal bound whenever its "runtime" trip-count variable is itself traceable to a compile-time-constant expression (a literal, or a ternary between two literals) — Apos.Shapes' Newton-iteration SDF turned out to be exactly this case (`newton_steps = converged ? 0 : 12`), so `apos-shapes.fx` no longer emits `SD0402` either, confirmed end-to-end through the real CLI with the GL render-proof (`validation/VsDriven -- apos`) still matching the `mgfxc` golden unchanged. **A genuinely unfixable case remains** — a loop bounded by a plain runtime uniform with no compile-time ceiling anywhere in the shader — pinned by a fresh regression fixture, `examples/Sd0402UniformBoundedLoop.fx`, which still warns through the real CLI (see `plan/DONE/PHASE-53-error-visibility-and-gl-lowering.md` D7). **SD0403 (0.15.0)** closes the version-gate half of the same family: a GLSL-1.30+/ES-3.00-only construct that survived into the **versionless** emitted GL source (`transpose`, the `sinh` family, `isnan`/`isinf`, `texelFetch`, bit-cast builtins, `switch`, `uint` types/literals, non-square matrices, integer shifts, and the integer bitwise/modulo operators `&` `|` `^` `~` `%`) is now flagged at compile time instead of failing at `Effect`-load only on strict front ends (macOS OpenGL, Mesa, WebGL1) — the class behind issues #149 and #163. It also backstops the Rule 8 / trunc lowerings by flagging any `round`/`roundEven`/`trunc` call a rewrite missed. |
| **Unrecognized vertex-input semantic defaulted with no warning (`SD0104`)** | **CLOSED 2026-07-31 ([Phase 51](../plan/PHASE-51-consolidated-remainder-backlog.md) A11, bug-hunt 2026-07-27 N5)** | `VertexSemanticMapper` defaults an HLSL vertex semantic it does not model to `VertexElementUsage.TextureCoordinate`. The **value is right** (real `mgfxc` defaults identically and the fallback bytes are unchanged), but `mgfxc` also **warns when it defaults** and ShadowDusk did not, so a typo (`TEXCORD0` for `TEXCOORD0`) silently minted a *phantom* TextureCoordinate attribute that MonoGame's `VertexInputLayout` then demanded from the vertex declaration, with a failed draw as the only symptom. Now surfaced as the **`SD0104`** warning on the two targets that write this table, Vulkan (SPIR-V) and DirectX12 (DXIL), through `CompiledShader.Warnings` / CLI stderr / `ValidateAsync`. A **warning, never an error** (drop-in parity: `mgfxc` accepts and defaults). Pinned by `VertexSemanticMapperTests`, `SpirvVertexInputReflectorTests`, and end-to-end `SD0104` tests in `VulkanEffectCompilerTests` / `DirectX12EffectCompilerTests`; no render gate is owed, because no emitted byte moves. |
| **Issue [#149](https://github.com/kaltinril/ShadowDusk/issues/149) — OpenGL profile emits `isnan()` into versionless GLSL, rejected on macOS** | **FIXED 2026-07-23** | DXC compiles HLSL `min`/`max`/`clamp` to NaN-aware SPIR-V (`NMin`/`NMax`/`NClamp`); SPIRV-Cross's stock GLSL lowering for those is an `isnan(...)`-ternary, valid only in GLSL 1.30+. ShadowDusk's OpenGL profile emits **versionless** GLSL (correct for the legacy MonoGame GL runtime otherwise) with no `#version` directive, so `isnan` was unavailable — desktop NVIDIA/AMD/Intel drivers (Windows/Linux) accepted it leniently, Apple's strict GLSL compiler rejected it outright, and every affected shader failed to load on macOS. Confirmed on `apos-shapes.fx` (28 `isnan(` occurrences pre-fix vs zero in the real mgfxc golden). **Fixed** by setting SPIRV-Cross's `RELAX_NAN_CHECKS` compiler option to `true`, unconditionally, in `SpirvCrossGlslTranspiler` — matching `mgfxc`'s own zero-`isnan` output exactly (the faithful choice, not a flag/relaxation). Verified: `apos-shapes.fx` 28→0 and `apos-shapes-sm6.fx` 147→0 `isnan(` occurrences; the Phase 51 A3 render-proof still passes at maxd 2/255 (unchanged — a lowering-strategy change, not a math change); full `dotnet test` green with **zero byte changes** in the cross-host byte-identity manifest (no existing corpus fixture had actually triggered this lowering before). Pinned by `ThirdPartyShaderCorpusTests.AposShapes_OpenGl_EmitsNoIsnan_Issue149`. Full writeup: [`plan/DONE/ISSUE-149-gl-isnan-versionless-glsl.md`](../plan/DONE/ISSUE-149-gl-isnan-versionless-glsl.md). |

---

## 8. ShaderToy / GLSL frontend — a DISTINCT evidence axis (NOT `mgfxc`-equivalence)

`src/ShadowDusk.ShaderToy` (Phase 47) converts a single-pass ShaderToy / GLSL image shader to a
self-contained `.fx`, which the **existing, unchanged** pipeline then compiles. Its evidence bar is a
**separate rung from the headline `mgfxc`/`fxc`-equivalence bar** and must never be conflated with it:

- **There is NO `mgfxc` oracle for ShaderToy input** — a `.glsl` is not an `mgfxc` input, so there is
  nothing to be byte/render-equivalent *to* on the input side. The frontend's honest bar is
  **pixel-fidelity vs the ORIGINAL GLSL**: render the original ShaderToy GLSL in a raw GL context
  (ground truth) vs OUR converted `.fx`→`.mgfx` through MonoGame, diff per pixel. Phase 46's
  out-of-band `render-proof --fidelity` gate: **46/46 deterministic shaders match the original at mean
  0.00/255** (gallery 72/72). This driver stays out-of-band like `validation/*`.
- **The DOWNSTREAM half is unchanged.** Once the converter emits `.fx`, the `.fx`→`.mgfx`/`.fxb` step is
  the SAME faithful, `mgfxc`-equivalent pipeline proven by the existing corpus — the promotion changes
  neither bar (proven additive: existing `.fx` output byte-identical; converter is pure-managed, no
  native/MonoGame dep — guarded by `NoMonoGameInProductLibrariesTests`).
- **CLI `.glsl` route** (`ShadowDuskCLI shader.glsl out.mgfx /Profile:...`): backed by
  `CliShaderToyInputTest` (compile OpenGL/DX, located-reject error path, byte-identity CLI ≡
  Convert+pipeline).
- **The route is render-proven on OpenGL since 2026-07-29** (Phase 51 A5, `validation/ShaderToyRouteGl`,
  in CI). The oracle question above cuts finer than "no oracle exists": there is none for the
  ShaderToy **input**, but the converter's **output** is ordinary HLSL `.fx`, which `mgfxc` compiles
  like any other. So the route's downstream half *is* held to the reference compiler — ShadowDusk's
  build and `mgfxc`'s build of the same converted `.fx`, rendered in real MonoGame DesktopGL, at
  **maxd 0**. That is a strictly weaker claim than fidelity-to-the-original-ShaderToy and must not be
  read as replacing it.
- **A DirectX arm of that gate is not possible, and the reason is a real finding, not a limitation of
  the harness (2026-07-29).** The converter emits `vs_3_0`/`ps_3_0` in **both** branches of its
  `#if OPENGL` header, and real `mgfxc /Profile:DirectX_11` rejects that outright: *"Invalid profile
  'vs_3_0'. Vertex shader 'VSMain' must be SM 4.0 level 9.1 or higher!"* (MonoGame's DirectX profile
  floor; the stock convention is `vs_4_0_level_9_1` in the `#else` branch). **ShadowDusk compiles the
  same file for DirectX_11 successfully** — measured, exit 0, 2033 bytes. So there is no DirectX
  golden to diff against, *and* the pair is a drop-in reject-fidelity divergence in its own right.
  Both halves are filed as [Phase 51](../plan/PHASE-51-consolidated-remainder-backlog.md) **A10**;
  neither was changed while closing A5, because one moves every converter golden and the other
  changes the compiler's reject set.
- The Windows DX/FNA render-gate fixtures for this route remain a tracked follow-up (and the DX one is
  blocked by A10 above); run the Windows render gates on a GPU box before a release that ships this route.
- **Where the two out-of-band ShaderToy drivers live, and their exact commands** (neither runs under
  `dotnet test`; both need a real GPU, so neither is in CI or in `run-windows-render-gates.ps1`):
  - **Fidelity / gallery / multipass render-proof** (the §8 bar above) —
    `dotnet build src/ShadowDusk.Cli/ShadowDusk.Cli.csproj` first, then
    `dotnet run --project tools/shadertoy2fx/render-proof/ShadowDusk.ShaderToy.RenderProof.csproj`
    (add `-- --fidelity` for the pixel-fidelity gate, `-- --gallery` for the montage).
  - **Interactive viewer sample's headless self-test** (a *sample* smoke, deliberately NOT a product
    gate — it is reach, not the bar) — `dotnet run --project samples/ShaderToyViewer -- --smoke`.
    Moved here from `tools/shadertoy2fx/sample/` by Phase 51 A4.
- **The DirectX arm now exists — the blocker was a real defect and it was fixed (2026-07-31, Phase 51
  A10).** It used to read *"a DirectX arm of that gate is not possible"*, and the reason was a finding
  rather than a harness limitation: the converter emitted `vs_3_0`/`ps_3_0` in **both** branches of its
  `#if OPENGL` header, which real `mgfxc /Profile:DirectX_11` rejects outright (*"Invalid profile
  'vs_3_0'. Vertex shader 'VSMain' must be SM 4.0 level 9.1 or higher!"*) while **ShadowDusk compiled
  the same file successfully**. Both halves are now closed: the converter emits an `SM4`-gated header
  whose DirectX arm names the `*_4_0_level_9_1` pair (verified compilable by the pinned `mgfxc` for
  `DirectX_11`), a `DirectX_11` golden is committed for the pinned fixture, `validation/ShaderToyRouteDx`
  is the DX arm of the gate, and the DirectX target now rejects sub-floor profiles itself (`SD0015`).
  The OpenGL arm is unaffected: ShadowDusk's GL bytes for this fixture are **byte-identical** before and
  after, and the `mgfxc` `OpenGL` golden regenerated byte-for-byte identical too.
- The Windows **FNA** render-gate fixture for this route remains a tracked follow-up; run the Windows
  render gates on a GPU box before a release that ships this route.

### 8.1 Reject-fidelity: mgfxc's per-target compile-profile floor (measured 2026-07-31)

Every `KnownProfiles` entry was swept through the **pinned** `mgfxc` (`dotnet-mgcb` 3.8.4.1 —
§6.1's oracle pin) for each of its three profiles. This is the authoritative record of what the
reference compiler accepts, and of how much of it ShadowDusk enforces today.

| mgfxc profile | Accepts | Rejects, with | Enforced by ShadowDusk? |
|---|---|---|---|
| `DirectX_11` | `{vs,ps}_4_0_level_9_1`, `_level_9_3`, `_4_0`, `_4_1`, `_5_0` | *"Invalid profile 'X'. Vertex/Pixel shader 'E' must be SM 4.0 level 9.1 or higher!"* — **including `_level_9_0` and every SM6 profile** | ✅ **yes** — `SD0015` (Phase 51 A10) |
| `OpenGL` | SM ≤ 3 (`{vs,ps}_1_1`…`_3_0`, `_3_sw`; `ps_1_x` is refused by fxc itself) | *"Invalid profile 'X'. Vertex shader 'E' must be SM 3.0 or lower!"* for `_4_0`/`_4_1`/`_5_0`; the `_4_0_level_9_*` forms die later inside MojoShader/fxc instead | ❌ **gap** — ShadowDusk still accepts SM4+ on OpenGL |
| `Vulkan` | `vs_6_0` / `ps_6_0` only | *"Invalid Vulkan vertex profile 'X'! Requires vs_6_0."* | ❌ **gap** — ShadowDusk accepts any recognized profile on Vulkan |
| `DirectX_12` | **not measured** — it exists only in mgfxc **3.8.5**, which is not the pinned golden oracle and is not installed here | — | ❌ **unmeasured**, deliberately not guessed |

The two open gaps are the same class as `SD0015` and each carries its own reject-set blast radius,
so each needs its own corpus sweep before it lands. For scale, measured at the same time: enforcing
the **OpenGL** ceiling would newly reject 7 root fixtures (`Minimal.fx`, `cbuffer.fx`, `textured.fx`,
`multipass.fx`, `multitechnique.fx`, `platform-macros.fx`, `basiceffect-mini.fx`), every one of which
real `mgfxc /Profile:OpenGL` already refuses.

---

## Update history

One line per update, newest first; the cells above are always the current truth and the full detail lives in the linked phase docs.

- **2026-08-02** — **Issue #189: DirectX is deliberately NOT being fixed, and that is a measured decision rather than a deferral.** DX11 emits sampler slots 0/1 where `mgfxc` emits the declared 2/3 (visible as the `SamplerRegisterSparse [DirectX_11]` cell in the Phase 41 divergence matrix). The obvious fix ships a regression: on DX11 the record's `samplerSlot` is where MonoGame assigns baked sampler state, so it must name the sampler register the **shipped bytecode reads** — and since `FxPreParser` drops the clause before DXC, our DXBC reads 0/1. Writing 2/3 would send baked state to a register the shader never samples. Making the bytecode itself use 2/3 means re-emitting the clause into the rewritten HLSL, which moves the DirectX/DX12/Vulkan/FNA bytes for a defect none of them have. The sibling divergence (fxc auto-assigns DX **texture** registers by first use, we use declaration order) is not closable either — that allocation lives inside each compiler's own bytecode. DX11's records are internally consistent with DX11's bytecode, which is the property that matters, and no DirectX failure has been reported. Recorded in `project_decisions.md` and `plan/DONE/ISSUE-189-gl-sampler-slot-declaration-order.md` §6.3; revisit only on a reported symptom.
- **2026-08-02** — **Issue #189 sparse half closed: an explicit `register(sN)` on a LEGACY `sampler` declaration now pins the OpenGL texture unit, and the modern spelling deliberately still does not.** Samplers at `s2`/`s3` with nothing at `s0`/`s1` were compacted to units 0/1 — order-preserving, internally self-consistent, and still wrong, because `SpriteBatch` overwrites unit 0 with the sprite after `EffectPass.Apply()`, so the sampler pushed onto it read the sprite. `FxPreParser` now RECORDS the register index it already parsed (`FxParseResult.ExplicitGlSamplerSlots`, texture-keyed) before its SM4 rewrite drops the clause; `ResolveSlots` prefers it. **Recorded, not re-emitted** — putting the clause back into the rewritten HLSL would change what DXC compiles and move the DirectX/DX12/Vulkan/FNA bytes for a defect none of them have. **The legacy-only scope is measured, not a limitation:** `mgfxc` emits `ps_s2` at unit 2 for `sampler X : register(s2);`, but for `Texture2D T : register(t3); SamplerState S : register(s2);` its OpenGL build puts the pair on unit 0 regardless, allocating by texture declaration order — so honouring modern registers would have been a DIVERGENCE, and both behaviours are now pinned by tests that fail if either is "corrected" later. **A pre-existing divergence surfaced while fixing it:** the declaration rank was taken from the SPIR-V `Binding` decoration, which equals declaration order only while DXC auto-allocates and equals REGISTER order once annotated, so `Texture2D TexA : register(t3); Texture2D TexB : register(t2);` put TexB on unit 0 where mgfxc puts TexA. The rank now comes from module order. That one was caught by an adversarial "modern registers must NOT be honoured" test, not by any render. Rung 4: the new **"sparse" arm** of `validation/SamplerRegisterOrderGl` (§6), committed RED first at maxd 255 / 4096 px and green at **maxd 0**. Full suite **3139/3139** (+7 tests), Windows render gates **15/15**, all eight in-process GL gates green, and the 48 OpenGL byte-identity manifest entries **still unchanged**. The reporter's own vendored `apos-shapes-aa.fx` (explicit `s0`, an unannotated sampler, explicit `s2`) is byte-identical to its mgfxc golden through both fixes. Remaining: the DirectX 11 sampler slot ignores `register(sN)` (§7), left alone because fxc itself auto-assigns DX texture registers by first use.
- **2026-08-02** — **GitHub issue #189: OpenGL sampler SLOTS were allocated in SPIRV-Cross's first-use order where fxc allocates in HLSL DECLARATION order. Fixed, render-proved, and it retracts a claim this file used to make.** A `sampler : register(s0)` that was not *also* sampled first landed on texture unit 1. Because `SpriteBatch` forces the sprite onto unit 0 immediately after `EffectPass.Apply()`, the sampler ShadowDusk put on unit 0 read the sprite while the one it put on unit 1 read whatever nothing had bound — a silent wrong-texture render in the single most common MonoGame custom-effect idiom. Measured against the pinned `mgfxc` 3.8.4.1 on the reporter's own repro: mgfxc `ps_s0`→`SpriteSampler`, ShadowDusk `ps_s0`→`MaskSampler`. **The rule was measured, not assumed**, on three shapes including one with every `register` annotation stripped — fxc allocates in declaration order in all of them, annotated or not, so this is not merely about honouring `register(sN)`. Slots now come from `SpirvCombinedSamplerPairs.ResolveSlots` (the pair's texture declaration index, ranked by the raw SPIR-V `Binding` DXC allocates in declaration order), used by BOTH the GLSL rewriter and the `.mgfx` table so they cannot drift; records are emitted sorted by slot like mgfxc's. **The validation-gap lesson is the durable part:** every one of the then-30 GL render gates bound its textures through `effect.Parameters[...]`, under which a first-use table is internally consistent and renders *correctly* — so no gate could see this. Worse, `validation/SamplerPairsGl` arm B was documented **in this file** as proving the numbering divergence was "benign"; it could not, because its two textures hold identical pixels by design, which is exactly what makes it blind to slot assignment. That wording is now retracted in its §6 row, in `project_facts.md`, and in the driver itself. New gate `validation/SamplerRegisterOrderGl` (§6) leaves unit 0 to `SpriteBatch` and uses distinct textures; measured **maxd 255 / 4096 px RED before the fix and maxd 0 after**. **No existing output moved**: all 48 OpenGL entries of the cross-host byte-identity manifest are unchanged, and the corpus goldens are untouched — only shaders whose declaration order differs from their sampling order change, and none in the corpus did. Full suite 3123/3123; Windows render gates **15/15**; the eight in-process GL gates all green. **Residual, open and measured:** a sparse/offset explicit register set (`s2`/`s3` with no `s0`/`s1`) still compacts to units 0/1 where mgfxc emits 2/3 — DXC's SPIR-V `Binding` namespace is flat and auto-allocated, so the explicit register number is not recoverable from the SPIR-V and closing it needs the clause threaded out of `FxPreParser`, which drops it today. Contiguous-from-zero sets, which is every SpriteBatch custom effect and the entire corpus, are exact. DirectX 11 and Vulkan allocate their own slots by their own rules and were deliberately left alone (§7).
- **2026-07-31** — **The DX12 Apos.Shapes `maxd 1` is root-caused: it is the pinned DXC build, and the two things that made it look like a shader bug were both harness bugs.** The delta itself is real and unchanged, but it is not a ShadowDusk defect. ShadowDusk compiles DX12 DXIL with `dxcoob 1.7.2212.40 (e043f4a12)` (the `Vortice.Dxc` 3.3.4 pin); the `mgfxc` golden was built with `dxcoob 1.8.2505.32 (b106a961d)` (MonoGame 3.8.5's bundled DXC). Feeding ShadowDusk's **own** pre-parsed HLSL and **own** DXC flags to a DXC 1.8 build reproduces the golden's pixel-shader DXIL **instruction-for-instruction** (a three-line `dxc -dumpbin` diff: shader hash, `!llvm.ident`, `!dx.valver`; with `-Qstrip_reflect` the container matches at the same 41876 bytes and the same six parts at the same offsets), and swapping only that DXIL payload into ShadowDusk's own `.mgfx` and rendering it gives **maxd 0, zero differing pixels of 402,984**. The two DXC builds emit identical DXIL intrinsic counts and differ only in `fast`-math-licensed rewrites (1.7 if-converts more; commutative `fmul` reordering; `x - y*c` → `x + y*(-c)` folds); it reaches a pixel only because the shader adds ±half an 8-bit LSB of dither immediately before quantization. **Two harness bugs are fixed here, and both had actively misled the two previous investigations.** (1) `AposGalleryRenderer.Cells` built its per-cell rectangles from the **untransformed** layout while the scene renders through a 1.15x scale + (6,4) translate — so the per-cell breakdown named whichever cell the pixels *landed* in, not the shape that made them. That is why this delta was recorded first as `DrawCircle`/`FillArc` and later as `FillRing`, and why "what do those shapes share?" had no answer: they were never the shapes involved (the pixels are `DrawEllipse`'s, verified by centroid). (2) The render target was sized to the *untransformed* 600x500 layout, so that same 1.15x scale pushed the whole last column off the right edge and the last row down to a ~10px sliver — **10 of the 30 cells contributed no pixels to any comparison**, while the GL visibility check still reported 30/30 because it was measuring those same untransformed rectangles. Target is now sized to the transformed extent (696x579); the gallery has no stored reference images, so no golden regeneration was needed. Re-verified after both fixes: DX11 **maxd 0** (both arms), Vulkan **maxd 0**, GL **30/30 genuinely visible**, DX12 **maxd 1 on 11 pixels** with the DXC-1.8 probe at **maxd 0**. Also found and deliberately **not** fixed: our DX12 DXIL still carries a `STAT` part the reference strips (§7). New diagnostic `validation/DumpPreprocessedHlsl`; new opt-in `SHADOWDUSK_DX12_PROBE_MGFX` arm on the DX12 gate. Validation-only: no `src/` change, no output-byte churn.
- **2026-07-31** — **Phase 51 A11: an unrecognized vertex-input semantic now warns (`SD0104`), closing the last half of bug-hunt 2026-07-27 N5.** `VertexSemanticMapper` has always defaulted a semantic it does not model to `VertexElementUsage.TextureCoordinate`, which is correct (real `mgfxc` defaults the same way), but `mgfxc` also prints a warning when it defaults and ShadowDusk did not. A typo (`TEXCORD0` for `TEXCOORD0`) therefore silently minted a phantom TextureCoordinate attribute that MonoGame's `VertexInputLayout` then demanded from the consumer's vertex declaration, and the only symptom was a failed draw with no reference back to the shader. The deferral reason recorded in the bug hunt — the mapper is pure and has no warning channel — stopped being true when Phase 53 added `CompiledShader.Warnings`. `Map(semantic, out bool recognized)` now reports the fallback; the Vulkan (SPIR-V) and DirectX12 (DXIL) attribute-table reflectors turn it into the warning; `CompilationPipeline.CompileEntryPoint` threads it into the `Warnings` element it already returns, so it reaches `CompiledShader.Warnings`, CLI stderr, MGCB, and `ValidateAsync`. It is a **warning, never an error** (drop-in parity: `mgfxc` accepts and defaults), and **no emitted byte moves** — the fallback usage/index values are unchanged and warnings never gate output, which is why this closed with unit + integration tests and no new render gate.
- **2026-07-31** — **Phase 51 A10: the converted-ShaderToy DirectX blocker is fixed on BOTH sides, and the DirectX target now enforces mgfxc's profile floor.** (1) `ShaderToyConverter` used to emit `vs_3_0`/`ps_3_0` in *both* arms of its `#if OPENGL … #else … #endif`, so every converted shader was un-buildable by `mgfxc /Profile:DirectX_11`. It now emits an **`SM4`-gated** header whose DirectX arm names `vs_4_0_level_9_1`/`ps_4_0_level_9_1` — `SM4` rather than the stock `#if OPENGL … #else` split because the `#else` arm also catches ShadowDusk's **FNA** target, which is capped at SM3; gating on `SM4` (the macro MonoGame's own DirectX_11 profile defines) keeps OpenGL *and* FNA on the legacy profiles. Verified compilable by the pinned mgfxc on both its profiles. **81 converter goldens + 2 multipass goldens moved (text only); the ShaderToy fixture's `.mgfx` bytes did NOT** — ShadowDusk's OpenGL build of `GradientToy.fx` is byte-identical before and after, and mgfxc's `OpenGL` golden regenerated byte-for-byte identical, so the CI GL gate is untouched. (2) A **`DirectX_11` golden now exists** for the pinned fixture and `validation/ShaderToyRouteDx` is the DX arm of the A5 gate, wired default-ON into `run-windows-render-gates.ps1` — **wired but NOT YET RUN**; the first green run is owed. (3) The reject half: `SD0015` rejects a recognized-but-sub-floor compile target on DirectX, from the accepted set measured across every `KnownProfiles` entry against the pinned mgfxc (§8.1). **20 corpus fixtures flipped from ACCEPT to REJECT on `DirectX_11`** — 14 vendored Nez shaders, `FnaMultiPassStates.fx`, `examples/Ex{Int,Mat3}UniformMember.fx`, plus 3 purpose-built fixtures; each was verified to be rejected by real mgfxc first. **No output bytes moved anywhere** (whole-corpus before/after sweep, 5 targets), and `FnaMultiPassStates.fx` was dropped from the DirectX arm of the byte-identity manifest because it pins DirectX bytes mgfxc cannot produce. The OpenGL SM ≤ 3 ceiling and the Vulkan `*_6_0` floor are the same class and stay open with their measurements recorded.
- **2026-07-31** — **[Phase 29](../plan/DONE/PHASE-29-mgcb-content-processor-plugin.md) closed: MGCB integration now exists, and it has a gate.** `ShadowDusk.MgcbPlugin` went from a `.csproj` with zero `.cs` files to a shipping content-processor plugin, which closes the §7 "MGCB Tier-1 `mgfxc` on `PATH`" row — not by making the `PATH` route work (it cannot), but by replacing it. New §6 row: `validation/MgcbPlugin`, a **delivery-shape** gate rather than a render one, default-ON in `run-windows-render-gates.ps1`. It runs a real `dotnet mgcb` content build through the `/reference:`d plugin and asserts the `.mgfx` payload inside the `.xnb` is **byte-for-byte the `ShadowDuskCLI` binary's**, that the `.xnb` envelope equals MGCB's own stock build's, and that the payload **differs** from stock. 7/7 cases across `/platform:DesktopGL` and `/platform:Windows`; identical payload out of `dotnet mgcb` 3.8.2.1105 / 3.8.3 / 3.8.4 / 3.8.4.1 / 3.8.5 and out of the packed `.nupkg` extracted to a bare directory. The in-suite half (`MgcbPluginByteIdentityTests`, 14/14) does run in CI. No render evidence changed and none is claimed: the bytes are identical to the CLI's, whose renders are already rung-4 proven.

- **2026-07-30** — **Release 0.16.0 docs pass: three CI/evidence claims in this file were stale and are corrected.** (1) The CI GL-gate list said **three** gates (`StateFidelity` / `CbufferModel` / `TextureBreadthValidation`) in two places (§"test programmatically" and the §7 promote-to-CI row); `validation-render.yml` actually builds and runs **seven** — those three plus `ReservedWordGl`, `SamplerPairsGl`, `DeferredSpriteMrtGl`, `ShaderToyRouteGl`. The same stale three-gate list was corrected in `validation/run-windows-render-gates.ps1`'s own `.DESCRIPTION`. (2) The 2026-07-28 Phase 52 entry described `ForwardCompat` as a three-runtime matrix; it is now a **seven-release sweep** (3.8.1.263 … 3.8.5, 70 renders, all maxd 0) with the floor **measured** at 3.8.1.263 — a supersede note was added rather than rewriting history. (3) The header date was two revisions behind its own body. Also this release: the OpenGL/DX12 sampler-pair fix (A7 below), `net8.0`+`net10.0` multi-targeting, and the FluentAssertions→Shouldly migration; no render-gate cell changed status. Windows render gates re-run for the release: **14/14 PASS** including FNA and both Vulkan gates.
- **2026-07-29** — **Phase 51 A7: OpenGL and DirectX 12 sampler records are now keyed on (texture, sampler) PAIRS — two defects closed, one of them silent.** (a) Several textures read through **one shared `SamplerState`** (the classic diffuse+lightmap shape, ordinary HLSL `mgfxc` has always compiled) was rejected outright with `SD0216`. (b) A **silent** sibling that guard could never see: SPIRV-Cross declares combined samplers in **first-use** order, so a shader sampling two textures in reverse order produced matching counts — guard satisfied — while both the texture parameter and the sampler-type byte came out **swapped** (probed with a `Texture2D`+`TextureCube` pair: the GLSL declared `ps_s0` as `samplerCube` while the record claimed 2D, so MonoGame bound a 2D texture to a cube sampler unit, with no diagnostic). **DirectX 12 had defect (a) silently too** — it was never in the texture-keyed branch, so the second texture was simply never bound; DirectX 11 was always correct. The pair list is derived in **pure managed code** from the SPIR-V (`SpirvCombinedSamplerPairs`), not via SPIRV-Cross's `spvc_compiler_get_combined_image_samplers`, because the browser host's `spirv-cross.wasm` does not export it — a native call would have fixed desktop only and broken the CLI/browser byte-identity guarantee. `SD0215` and `SD0216` are **retired** (do-not-reuse); the new `SD0217` covers the shapes the ordering model does not cover plus an internal cross-check of the derived pair count against the sampler uniforms the emitted GLSL actually declares. **Rung 4:** `validation/SamplerPairsGl` (see its §6 row for what each arm discriminates and why a symmetric scene would have proven nothing). Golden corpus and byte-identity manifest **untouched** — every 1:1 texture/sampler shader is byte-identical; output moved only for shapes that previously failed to compile or were silently mis-bound.
- **2026-07-29** — **Phase 51 A2: the DeferredSprite MRT render rung is closed — the repo now has a gate that binds more than one render target.** Phase 41 GAP-2 (Nez's `DeferredSprite.fx` failing on GL with `Semantic COLOR is invalid`) was fixed and structural-matched in June 2026, but its render rung stayed open for a stated reason: *every* GL render gate in this repo binds a single render target, so nothing we ran could tell "`COLOR1` reached attachment 1" from "the second output went nowhere" — and a structural match cannot see it either, because the second output lives in the emitted GLSL (`gl_FragData[1]`), not in the `.mgfx` record tables. New driver `validation/DeferredSpriteMrtGl` binds two render targets on real MonoGame DesktopGL, draws the shader, reads **both** attachments back, and pixel-diffs each against the real `mgfxc` `OpenGL` golden: **maxd 0 on both**, first run. Wired into `validation-render.yml` (CI, Mesa llvmpipe). The scene is built so each failure mode is a distinct, named outcome rather than "wrong colour": 1:1 texel-to-pixel drawing removes filtering from the picture, the shader's own `clip()` splits the surface in one draw, and attachment 1's alpha is put through the shader's arithmetic to a value no clear colour and no copy of attachment 0 can produce. **The mutation check justified keeping an absolute arm alongside the diff arm:** binding one target instead of two leaves the mgfxc-diff reporting maxd 0 — both sides broken identically — and only the absolute arm turns red. That arm also runs against the golden itself, so a future diff failure is attributed to the right side instead of blaming the candidate by default. Validation-only: no `src/` change, no output-byte churn.
- **2026-07-29** — **Phase 51 A5: the ShaderToy / `.glsl` route is render-proven on OpenGL, and closing it surfaced a reject-fidelity divergence.** New driver `validation/ShaderToyRouteGl` converts `GradientToy.glsl` in process with the real `ShaderToyConverter`, pins the result against a committed `.fx`, and pixel-diffs ShadowDusk's OpenGL build against **`mgfxc`'s build of that same converted `.fx`** on real MonoGame DesktopGL: **maxd 0**, in CI on Mesa llvmpipe. The oracle exists because §8's "no `mgfxc` oracle" applies to ShaderToy *input*; the converter's *output* is ordinary HLSL. The pinning matters: without it, converter drift would leave the golden describing a different shader and the diff would compare two unrelated builds. The harness drives a real **NDC fullscreen quad** rather than `SpriteBatch`, because the converted effect is VS-driven and passes POSITION through untransformed. **Finding (filed as A10, deliberately not fixed here):** the converter emits `vs_3_0`/`ps_3_0` in *both* branches of its `#if OPENGL` header, which real `mgfxc /Profile:DirectX_11` rejects (*"must be SM 4.0 level 9.1 or higher"*) while **ShadowDusk compiles it successfully** — so there can be no DirectX arm of this gate, and the accept/reject split is a drop-in divergence of the Phase 48 class. The new fixture also joined the auto-globbed corpus, which is what surfaced it.

- **2026-07-28** — **Phase 52 closed: MonoGame 3.8.5 stable is a proven member of the support matrix, and two pieces of validation infrastructure were found broken and fixed.** (A) `validation/ForwardCompat`'s matrix was widened to `3.8.2.1105` / `3.8.4.1` / **`3.8.5`**; the same unchanged v10 bytes render **maxd 0 across all three runtimes**, 10/10, within tolerance of the mgfxc goldens. *(Superseded 2026-07-30: the matrix now covers **seven** consecutive releases — 3.8.1.263, 3.8.1.303, 3.8.2.1105, 3.8.3, 3.8.4, 3.8.4.1, 3.8.5 — 70 renders, all maxd 0, and the floor is measured rather than assumed: 3.8.0.1641's loader predates MGFX v10 and rejects our output, making **3.8.1.263** the true floor.)* (B) `validation/MonoGameV11` re-pinned off `3.8.5-preview.6` to **3.8.5 stable** and re-run: result table unchanged cell for cell, so the MGFX v11 claim no longer rests on a pre-release runtime. (C) The mgfxc **oracle pin** was measured rather than assumed and turned out to be doubly broken — the "highest mgfxc in the cache" probe selected `dotnet-mgcb-editor-windows` 3.8.4.1's `mgfxc.exe`, which cannot run at all (ships without `SharpDX.D3DCompiler.dll`), while mgfxc 3.8.5 emits **MGFX v11** and matches 0 of 46 goldens. `tools/compile-fixtures.ps1` now resolves the pinned `dotnet-mgcb` mgfxc, asserts the container version byte it writes, and (separately) actually finds the shaders on a no-argument run — a `??`-on-empty-string bug meant a bare invocation had been globbing 0 files; `validation/ReservedWordGl` uses the same pin. See §6.1. (E) The documented **Tier-1 "`mgfxc` on PATH" MGCB override does not fire on any supported MonoGame version** — measured with a real logging `mgfxc.exe` shim against `dotnet mgcb` 3.8.2.1105/3.8.4.1/3.8.5 (0 invocations each, valid `.xnb` each time); MGCB compiles effects in-process. 3.8.5's new code-centric Content Builder is a C# project over `MonoGame.Framework.Content.Pipeline`, so it has no external-tool seam either. Recorded as a §7 gap; it makes Phase 29 the real native-integration path. (F2) `validation/AndroidGl` pin moved to `MonoGame.Framework.Android` 3.8.5, build-verified only. Product untouched: pin stays 3.8.2.1105, default stays MGFX v10, zero golden/manifest churn, full `dotnet test` 2381/2381 green.
- **2026-07-27** — documentation correction: the header and the §6 DX11/DX12 gallery rows now record the 2026-07-23 oracle-arm switch to the real `mgfxc` goldens (the package's embedded effect is vkd3d-shader-compiled — kept only as the DX11 vkd3d arm's same-toolchain baseline) and mark the DX12 2-cell 1/255 delta as an **open follow-up**, not confirmed toolchain drift.
- **2026-07-23** — **Phase 55: Apos.Shapes render-proofs upgraded from one hand-built shape to the FULL `ShapeBatch` shape gallery.** Requested directly by the owner: the existing Apos.Shapes render-proofs (Phase 51 A3, Phase 54) each drew exactly one hand-rolled circle through a bespoke, manually-reverse-engineered vertex struct — a regression in, say, the rectangle corner-radius branch or the dash-style math would not have been caught by any render gate. Replaced with the REAL `Apos.Shapes` NuGet package (0.7.7, pinned commit `a85a31c` — confirmed byte-identical to the already-vendored `apos-shapes-sm6.fx` modulo one comment) as both harness and golden, via its `ShapeBatch(GraphicsDevice, Effect? effect = null)` effect-injection constructor: one `ShapeBatch` uses the package's own embedded precompiled effect (originally treated as the golden — see the correction below), a second uses a ShadowDusk-compiled effect (the candidate), both driven through the SAME public `Draw*`/`Fill*`/`Border*` calls across 30 gallery cells (every shape kind × Draw/Fill/Border, with gradient/dash/rotation/corner-radii variants). Confirmed no `.xnb` is involved anywhere — `ShapeBatch` reads a raw embedded byte resource and calls `new Effect(graphicsDevice, bytes)` directly. **Results (after the same-day oracle-arm correction):** disassembling the package's embedded DX11 effect showed it is itself vkd3d-shader-compiled — a valid same-toolchain baseline for the vkd3d arm, NOT an oracle — so the oracle comparisons diff against the real, locally-generated `mgfxc` goldens instead. DX11 (`d3dcompiler_47` vs the real `mgfxc` golden, and `vkd3d-shader` vs the embedded effect) and Vulkan (vs the package's DXC-family embedded effect): **maxd 0**; DX12 (vs the real local `mgfxc` golden): 28/30 cells at maxd 0, **2 cells at 1/255 — an open, not-yet-root-caused follow-up** (see the §6 rows); GL gets a candidate-only visibility check (30/30 shapes visible), since `mgfxc`'s own GL compile of this shader revision is the pre-existing confirmed MojoShader black-render bug (Phase 51 A3) — no trustworthy GL oracle exists for this gallery. The hand-rolled vertex/packing code was deleted from the DX11/DX12/Vulkan render-proof drivers (GL's existing single-shape proof, which targets a different, older fixture revision, is untouched). Purely additive validation-only change — no `src/` change, full `dotnet test` (2222/2222) green, zero output-byte churn. See `plan/DONE/PHASE-55-apos-shapes-shape-gallery-render-proof.md`.
- **2026-07-23** — **Phase 54, third pass: DX12 VS-driven crash root-caused and fixed — DX12 is now fully rung-4 proven.** The second pass (below) surfaced a real, unresolved `E_INVALIDARG` draw-time crash for any ShadowDusk-compiled DX12 vertex shader. Root-caused by fetching `upstream/MonoGame` fresh and checking out the real `v3.8.5` tag (2026-07-15) directly — the previously-used local clone was stale (its `develop` HEAD predated the actual DX12 shader-wrapper code, which is why grepping it for `0xB00B00` found nothing). Reading `VertexInputLayout.Native.cs`/`Shader.Native.cs` directly: MonoGame's shared `VertexInputLayout.GenerateInputElements` (used by every native backend, not just DX12) builds the D3D12 input layout by iterating the vertex shader's own per-shader vertex-attribute table; ShadowDusk's DirectX12 compile path left this table **empty**, so the per-attribute loop never ran, silently producing a zero-element input layout (its own "missing input" check lives inside that same loop, so it never fires either) — which then fails `CreateGraphicsPipelineState`, called lazily right before the first Draw. Fixed with `DxilVertexInputReflector` (mirrors the existing `SpirvVertexInputReflector`, shared semantic-mapping logic extracted into `VertexSemanticMapper`). Confirmed via a from-scratch standalone D3D12 repro (debug layer + GPU-based validation, MonoGame's exact root-signature layout) that PSO creation with ShadowDusk's shader bytecode was always clean — the input layout was the only problem. Also fixed two real (non-causal) divergences found via direct byte-level golden comparison along the way: the DXC root-signature-embedding flags added in the second pass were a red herring (the real golden's DXIL carries no `RTS0` part at all) and reverted; and a spurious 4th `SpriteTextureSampler` parameter (the real golden has 3) from `ParameterListBuilder` defaulting `includeSamplerParameters` to `true` for DX12, now `false` to match DirectX11's DXBC convention. `validation/VsDrivenDx12` (`vs` + `apos` modes): **maxd 0**, wired into `run-windows-render-gates.ps1`. PS corpus re-verified unaffected (10/10, maxd 0). Full `dotnet test`: 2222/2222 green.
- **2026-07-23** — **Phase 54, second pass: DX12 PS corpus is now genuinely oracle-proven; VS-driven found broken (superseded by the entry above).** Own-output-only evidence was rejected as a completion bar (parity with every other backend, which is all held to a real reference-compiler oracle). Found that `dotnet-mgcb` 3.8.5's own content builder (`mgcb.dll`) can build a real `DirectX_12` profile directly (`/platform:WindowsDX12`); generated real goldens for the PS corpus + `VsTransformColorTexture.fx` + `apos-shapes-sm6.fx`, checked into `tests/fixtures/golden/DirectX_12/`. Required widening the 10 base-corpus fixtures' `#if VULKAN` shader-model gate to `#if SM6` (DirectX12 sets `SM6` but not `VULKAN`) — behavior-preserving for every existing target, re-verified via a full `run-windows-render-gates.ps1` pass (all pre-existing gates still green). `validation/BaselineDx12` + `CandidateDx12` + `compare_dx12.py`: **maxd 0, 10/10** against the real golden — the PS/SpriteBatch corpus is done. Building the oracle also exposed that the prior pass's "no embedded root signature needed" conclusion only held for pixel-only passes (no `VertexShader` line, falls back to SpriteBatch's own VS); a genuine ShadowDusk-compiled DX12 vertex shader (new `validation/VsDrivenDx12`, both a generic VS rig and Apos.Shapes) crashes real `WindowsDX12` at draw time with a native `E_INVALIDARG`. Adding the missing DXC root-signature-embedding flags did not fix it. This is now a confirmed, open defect blocking VS-driven DX12 — not swept under the rug, not wired into the render gate script, and DX12 is not claimed "done" for VS-driven content.
- **2026-07-23** — **Phase 54 opened and DX12 own-output render-proven (superseded by the entry above)**: split out of Phase 52 Area D once source inspection found no `PlatformTarget.DirectX12` exists — a new backend, not a render-validation rung. Added `PlatformTarget.DirectX12`/`MgfxProfile.DirectX12` (profile 2, matching real MonoGame 3.8.5's `DirectX12ShaderProfile`), DXC compile routing (plain SM6 DXIL, no `-spirv`/`-Zpr`, reusing the existing DXIL reflection pipeline), and `DirectX12ShaderCodeWrapper` (the `0xB00B00`+2-int header MonoGame's native DX12 `Shader_Create` expects). `validation/CandidateDx12` renders the 10-shader PS corpus correctly on real MonoGame 3.8.5 `WindowsDX12` — 10/10, first attempt. No `mgfxc` `DirectX_12` golden existed yet at that point, so this was own-output proof only; full `dotnet test` green throughout.
- **2026-07-23** — **Phase 51 A3 (DX slice)**: Apos.Shapes DirectX render-proof. `validation/VsDrivenDx -- apos` renders `apos-shapes-sm6.fx` (the same fixture the Vulkan proof uses — its DX macro set takes the fixture's `#else` branch, so no new DX fixture variant was needed) through a bespoke vertex-buffer harness and pixel-diffs both DXBC backends against the real mgfxc `DirectX_11` golden: **maxd 0**.
- **2026-07-23** — **Phase 51 A3 GL slice**: `validation/VsDriven -- apos` render-proves Apos.Shapes on real MonoGame DesktopGL against the mgfxc OpenGL golden at maxd 2/255. Uses `apos-shapes.fx` (the Phase 49 pin), not the `apos-shapes-sm6.fx` fixture DX/Vulkan use — that revision's real mgfxc GL compile is confirmed to produce a wrong (solid-black) render (a MojoShader/fxc codegen bug, `-0.0 >= 0.0` evaluating false), independently confirmed by recomputing the shader's OkLab math in double precision. A3 is now closed on DX, Vulkan, and GL; FNA stays permanently excluded (SM3 instruction-slot ceiling, `SD0305`).
- **2026-07-23** — **Issue #149 fixed**: ShadowDusk's OpenGL profile was emitting `isnan()` (from DXC's NaN-aware `min`/`max`/`clamp` lowering) into versionless GLSL, which Apple's strict GL compiler rejects; found while closing the GL slice above. Fixed by defaulting SPIRV-Cross's `RELAX_NAN_CHECKS` option on for the whole OpenGL profile — zero byte changes anywhere else in the corpus. Full writeup: `plan/DONE/ISSUE-149-gl-isnan-versionless-glsl.md`, §7.
- **2026-07-23** — **Phase 53 closed** (Windows render gates 8/8, including the two KNI OpenGL gates that render its changed GL bytes: `KniVsDriven` maxd 0 in-process, `KniDesktopGL` within tolerance at max Δ 1 vs the mgfxc goldens and maxd 0 vs the MonoGame render). Post-review hardening: the SD0402 `while`-after-a-block false negative, a Vulkan binding collision where a pair inheriting an explicit register could land on an index another texture held, and `Gather*` calls and `Texture2DArray` / `SamplerComparisonState` declarations that the Vulkan pairing scan did not see.
- **2026-07-22** — **Issue #145 fixed and render-proven**: Vulkan matrices were packed row-major (every VS-driven effect rendered nothing) and legacy `tex2D` shaders access-violated on an unpaired synthesized texture. Added `validation/VsDrivenVulkan` — **the first Vulkan pixel-diff against the `mgfxc` golden (maxd 0)**, on a VS-driven transform and on the upstream `Apos.Shapes` reproducer — plus a corpus-wide device-free Vulkan structural gate. Both Vulkan gates are now default-ON in the Windows gate script. **Phase 53** landed the error-visibility work: verbatim compiler diagnostics on every surface, a `CompiledShader.Warnings` channel (the GL leg no longer forces `-WX`, restoring mgfxc parity), `Validate`/`ValidateAsync`, and the SD0400–SD0402 GL portability lint (§7).
- **2026-07-19** — Issue #136 (ANGLE D3D11 gradient poisoning) fixed: Rule 9a unwraps the entry-point one-shot wrapper (for-loop lowering demoted to fallback 9b); derivative-AA `apos-shapes-aa.fx` vendored + gradient-in-divergent-loop analyzer pin added; ANGLE-D3D11 render proof tracked as the remaining rung (§7).
- **2026-07-18** — DX12 added as an explicit ⬛ not-yet-supported column/row (unblocked by MonoGame 3.8.5 stable; research-first → [Phase 52 Area D](../plan/DONE/PHASE-52-monogame-3.8.5-support.md)). **Phase 32 Vulkan render-proven** on real MonoGame 3.8.5 DesktopVK, 10/10; the pixel-diff vs `mgfxc`'s own Vulkan output stays externally blocked (MonoGame `SlotOffset` bug, §7).
- **2026-06-28** — **Phase 50 Android on-device compile proven** (real API-34 emulator; §3/§7); `android-arm64` natives hosted + SHA-pinned + packed (shipping in 0.11.0).
- **2026-06-27** — Phase 49 Gum/Apos.Shapes regression corpus vendored; Phase 41 GAP-1 closed on FNA (7 stock effects compile); issue #107 do-while fix render-proven in KNI WebGL (both profiles); Phase 35 `__KNIFX__` macro; KNIFX-on-KNI-WebGL closed (§7).
- **2026-06-18** — Phase 45 B10 reserved-word free-uniform render-proven through MGFX v11 / KNI v10 / KNIFX.
- **2026-06-17** — Phase 45 B10 GL render gate; issue #106 parser regression fixtures.
- **2026-06-15** — Phase 44: KNI DirectX render-proven; GL render gates wired into CI.

---

## Sources / cross-references

- Engine state (June 2026, primary-source): [`DONE/PHASE-35-appendix/shader-pipeline-landscape-2026-06.md`](../plan/DONE/PHASE-35-appendix/shader-pipeline-landscape-2026-06.md).
- Evidence-ladder definition + backend pipeline table: [`the-purpose.md`](the-purpose.md).
- Forward-version (v11/KNIFX/Vulkan/DX12) status: [`PHASE-35-forward-version-support.md`](../plan/DONE/PHASE-35-forward-version-support.md).
- KNI WebGL render harness: `tests/ShadowDusk.BrowserTests/README.md` (Phase 24).
- Cross-OS byte-identity rationale: `tests/fixtures/golden/byte-identity/README.md`.
