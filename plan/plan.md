# ShadowDusk — Implementation Plan

This document is the top-level index. Each phase is fleshed out in its own document.

---

## THE PURPOSE (what every phase serves)

**The product is a drop-in `mgfxc` replacement: a self-contained library** a developer adds to a **MonoGame/KNI project on Linux, macOS, or Windows** that compiles **`.fx` → `.mgfx` in memory at runtime** with **nothing but the library** (no `fxc`, no Wine, no SDK), whose output **loads and renders identically to `mgfxc`'s in the real runtime**. **One faithful compiler — the same `mgfxc`-equivalent result everywhere.**

- The **library is the product**; the **CLI** and **MGCB plugin** are delivery shapes of it; the **browser / WASM shader-fiddle is only a sample of reach**, never the product.
- **No substitute compilers:** every host runs the *same* faithful pipeline (HLSL→DXC→SPIR-V→SPIRV-Cross→GLSL→MGFX, or vkd3d→DXBC). A host that can't yet run a faithful component is *not done* — never a licence to swap in a different compiler that diverges from `mgfxc`.
- Full statement + the success/evidence bar: see **`CLAUDE.md` → "THE PURPOSE" / "What success actually means"**.

Every phase below exists to serve that sentence. If a phase or sample starts redefining the goal, it has drifted — stop and re-anchor here.

---

## Reference Documents (background, not phase plans)

| Document | What it's good for | ⚠️ Read with |
|---|---|---|
| [`monogame_runtime_mgfx_compiler_research.md`](../monogame_runtime_mgfx_compiler_research.md) | Architecture survey of *how one builds a runtime MGFX compiler*: the `new Effect(gd, byte[])` loading path, `.xnb`-vs-raw-bytes, the DXC/SPIRV-Cross/MojoShader tool landscape, and a §0.1 map from its sections to these phases. | Its §0 alignment note — it was written greenfield and **understates** the MojoShader-GLSL-dialect blocker, which Phase 17 §3.6 proves is the real wall. Treat it as context, not a plan; Phases 7/8/17 supersede its roadmap. |
| [`ISSUE-70-gl-vertex-fidelity.md`](ISSUE-70-gl-vertex-fidelity.md) | The two OpenGL vertex-shader fidelity bugs from GitHub issue #70 — (1) the matrix transpose and (2) the legacy `: POSITION` output not mapped to `gl_Position` — their root causes, fixes, rung-4 evidence, the **validation-gap lesson** (identity-only / SV_Position-only inputs hid both), and the corpus matrix-sweep + mutation test that now guard them. | A *correctness*-fix record, not a version/format question — KNIFX/MGFX-v11 do not fix these (they're in the emitted GLSL). The MonoGame pin / MGFX v10 default are unchanged. |
| [`PHASE-35-appendix/shader-pipeline-landscape-2026-06.md`](PHASE-35-appendix/shader-pipeline-landscape-2026-06.md) | The **live-source (June 2026) state** of MonoGame's and KNI's shader pipelines: both still use **MojoShader for OpenGL**; the MojoShader limit is **OpenGL-only** (ShadowDusk's DirectX target already emits SM4/5 vertex-texture-fetch / texture-arrays the GL path rejects); the modern frontier is DirectX (now) + Vulkan/DX12 (MonoGame 3.8.5 preview). Also clears the "are we using the bad library?" confusion: ShadowDusk uses **DXC + SPIRV-Cross**, not MojoShader. | A snapshot of evolving forks — re-verify on each MonoGame/KNI release. Sibling design doc: `PHASE-35-appendix/knifx-autoselect-design.md` (the capability auto-detect/override architecture). |

---

## Completed Phases

These phases are fully implemented. Their documents have been moved to `DONE/` with all checklist items ticked.

| Phase | File | Summary |
|-------|------|---------|
| 0 ✓ | [DONE/phase-0-setup.md](DONE/phase-0-setup.md) | Fixture corpus (39 .fx / 4 .fxh), golden .mgfx reference compilation, ShaderViewer sample — **DONE** |
| 1 ✓ | [DONE/PHASE-1-solution-scaffold.md](DONE/PHASE-1-solution-scaffold.md) | .NET solution structure, project references, NuGet dependencies, test framework — **DONE** |
| 2 ✓ | [DONE/PHASE-2-fx9-pre-parser.md](DONE/PHASE-2-fx9-pre-parser.md) | Custom parser: extract technique/pass/sampler_state/render-state blocks before DXC sees the file — **DONE** |
| 3 ✓ | [DONE/PHASE-3-preprocessor-macro-injection.md](DONE/PHASE-3-preprocessor-macro-injection.md) | #include flattening, platform macro injection (MGFX=1, GLSL=1, SM4=1, etc.) — **DONE** |
| 4 ✓ | [DONE/PHASE-4-dxc-integration.md](DONE/PHASE-4-dxc-integration.md) | Vortice.Dxc wiring, per-platform DXC flags, HLSL → SPIR-V compilation — **DONE** |
| 5 ✓ | [DONE/PHASE-5-shader-reflection.md](DONE/PHASE-5-shader-reflection.md) | Cross-platform parameter metadata extraction via IDxcUtils::CreateReflection and SPIRV-Cross — **DONE** |
| 6 ✓ | [DONE/PHASE-6-spirv-cross-glsl-transpilation.md](DONE/PHASE-6-spirv-cross-glsl-transpilation.md) | SPIRV-Cross C API P/Invoke, SPIR-V → GLSL/MSL, Y-flip, depth range, combined samplers — **DONE** |
| 7 ✓ | [DONE/PHASE-7-mgfx-binary-writer.md](DONE/PHASE-7-mgfx-binary-writer.md) | .mgfx binary format serialization: header, constant buffers, shaders, parameters, techniques, passes — **DONE** |
| 8 ✓ | [DONE/PHASE-8-compiler-library.md](DONE/PHASE-8-compiler-library.md) | `ShadowDusk.Compiler` NuGet library — `EffectCompiler : IShaderCompiler`, pipeline orchestration, the consumer-facing package — **DONE** |
| 9 ✓ | [DONE/PHASE-9-cli-entry-point.md](DONE/PHASE-9-cli-entry-point.md) | dotnet tool CLI, mgfxc-compatible flags, MGCB error format, stderr routing, exit codes — **DONE** |
| 15 ✓ | [DONE/PHASE-15-integration-tests.md](DONE/PHASE-15-integration-tests.md) | End-to-end .fx compilation tests — 9 fixtures × 3 platforms, determinism, error cases (103 tests, all passing) — **DONE** |
| 16 ✓ | [DONE/PHASE-16-image-regression-tests.md](DONE/PHASE-16-image-regression-tests.md) | Visual regression tests — offscreen OpenGL rendering of all 9 Phase 15 fixtures, 12 reference PNGs anchored on ShadowDusk's own output, 13 tests passing — **DONE** |

---

## Active & Planned Phases

*(Status per each phase doc's own header. "In progress" = currently being worked; "Planned" = written but not started.)*

| Phase | Status | File | Summary |
|-------|--------|------|---------|
| 17 | ✅ Done | [DONE/PHASE-17-monogame-runtime-validation.md](DONE/PHASE-17-monogame-runtime-validation.md) | In-engine equivalence **complete (2026-05-30)** for the full SM3 PS-only corpus (all 10/10, Dissolve incl.): ShadowDusk `.mgfx` loads in a real MonoGame `Effect` and renders pixel-equivalent to `mgfxc` (OpenGL) — the fidelity (Part 2) bar. Carried forward: DirectX → Phase 18, VS-driven effects → backlog 17-VS |
| 18 | ✅ Done | [DONE/PHASE-18-directx-dxbc.md](DONE/PHASE-18-directx-dxbc.md) | In-engine equivalence **complete (2026-05-30)** for the SM5 PS-only corpus (all 10/10): ShadowDusk's DX `.mgfx` loads in real MonoGame WindowsDX and renders pixel-equivalent to `mgfxc`, via the cross-platform **vkd3d-shader** backend (`d3dcompiler_47` oracle on Windows) — the DirectX half of fidelity. Carried forward: VS-driven → backlog 17-VS, Linux/macOS *run* validation → Phase 30 CI |
| 19 | ✅ Done | [DONE/PHASE-19-wasm-runtime-compilation.md](DONE/PHASE-19-wasm-runtime-compilation.md) | WASM compile **engine** (scope narrowed 2026-05-30; browser-runtime tail → Phase 100). Built & desktop-verified: injectable backend seams; a pure-managed `SpirvReflector` proven equivalent to the DXIL oracle (10/10), removing the Windows-only reflection blocker; the GL pipeline reflects SPIR-V and emits `.mgfx` **byte-identical** to the DXIL path (10/10); `WasmShaderCompiler` composes the pipeline with `[JSImport]` DXC/SPIRV-Cross backends and **compiles for `net8.0-browser`** |
| 21 | ✅ Done | [DONE/PHASE-21-test-suite-performance.md](DONE/PHASE-21-test-suite-performance.md) | Resolved the 21m43s `ShadowDusk.Integration.Tests` outlier. Root cause (structural fit): a per-construction `dotnet publish -c Release` in `CliBinaryFixture` — a cold Release build + AV-scan of freshly-copied native binaries. Fix: reuse the normal build's CLI binary (PR #2); suite back to seconds (128/128, ~6 s). Dev-time Defender exclusions documented in `CLAUDE.md`. Guardrail added 2026-05-31: `ShadowDusk.runsettings` 5-min `TestSessionTimeout` so a future hang fails fast |
| 22 | ✅ Done | [DONE/PHASE-22-wasm-shader-fiddle-sample.md](DONE/PHASE-22-wasm-shader-fiddle-sample.md) | KNI Blazor-WASM **sample app** (`samples/ShaderFiddle.Web/`, net8.0-browser): paste `.fx` → compile in-browser → `new Effect` → cat + shader + error UI — **complete (2026-06-02)**. Both modes ship on the **faithful product pipeline**: mode-1 loads the 10 Phase-17 goldens; **mode-2 compiles in-browser via the faithful DXC→WASM frontend** (`WasmShaderCompiler`, self-registered from the `ShadowDusk.Wasm` package) — the original Slang frontend is **superseded by Phase 23** and now dead sample-only reference. Both caveats resolved: faithful frontend (Phase 23, byte-identical to desktop DXC) + render proven 10/10 in real headless KNI WebGL (Phase 24; `MGFXReader10` parses v10, no KNIFX-v11 needed) + a live desktop-browser run. Documented **stretch implemented** (reflect params → "Live parameters" panel) + Reset button; `build` **and** `publish -c Release` clean with the 17 MB `dxcompiler.wasm` bundled. Carry-forward: untrusted input → Phase 25 (never a P22 criterion) |
| 23 | ✅ Done | [DONE/PHASE-23-in-browser-compilation.md](DONE/PHASE-23-in-browser-compilation.md) | **Faithful in-browser compile is the PRODUCT — complete (2026-06-02), all 3 gates met (adversarial review: closeable, 0 blockers).** Option A: the pinned **DXC→WASM** (== desktop DXC `e043f4a1`) is `ShadowDusk.Wasm`'s in-browser HLSL→SPIR-V frontend, args forwarded verbatim — **G0/G1 byte-identity 10/10**, **no Slang leak** (Slang sample-only). **Turnkey**: Razor-SDK static web assets + library self-registration → a scratch consumer compiles with **only a `PackageReference`**, zero wiring. **Render-proven rung-4**: faithful in-browser compile renders **10/10 in real headless KNI WebGL** (real `Effect`/`readPixels`/`Effect.Parameters`), mgfxc-equivalence transitive via Phase 17. Fixed the .NET-8-WASM-no-MD5 reach bug (`ManagedMd5` ≡ BCL MD5). Full suite **515/515**. Carry-forwards: DirectX-in-WASM→4.1, CI/Linux wasm-rebuild→30 §16, VS-path→17-VS, untrusted input→25 |
| 24 | ✅ Done | [DONE/PHASE-24-browser-render-validation.md](DONE/PHASE-24-browser-render-validation.md) | **Browser render validation (Playwright headless) — complete (2026-06-01).** First real-browser run. Mode-1: **10/10 load + 10/10 render-equivalent** in real KNI WebGL vs DesktopGL of the same bytes. MGFX **v10 renders in KNI WebGL → KNIFX-v11 NOT needed for render parity (carry-forward CLOSED).** The initial Dissolve failure was a sample-side unset slot-1 sampler state (not a compiler bug; fixed in the sample/harness). Harness (`tests/ShadowDusk.BrowserTests/`) handed to Phase 30 §16. **Follow-up finding now FIXED:** ShadowDusk's *own* GL output emitted `roundEven()` (WebGL1 lacks it) → `Pixelated` failed to load; lowered to `floor(x+0.5)`, harness extended to validate ShadowDusk's own bytes (`--corpus=sd`), now 10/10. Carried forward: mode-2 sample verification → Phase 23 Gate 3 (restore-gated `slang-wasm.wasm`) |
| 25 | Reframed (doc-only) | [PHASE-25-security-hardening.md](PHASE-25-security-hardening.md) | **Security: state the trust model honestly.** Reframed 2026-06-12 — compiling a `.fx` is running code you chose to run (like compiling copied C++/C#); the shader author and the compiler-runner are the **same trust domain** (a developer building their game), so the original "untrusted-input" findings (path traversal, include/source size limits, macro validation, DLL-plant) were **removed as not-real-harm** (rationale recorded in the doc). Supply-chain pinning of the natives we ship is **already done** (Phases 37/40/4.1). Remaining work is **one `SECURITY.md`** stating the trust model + the consumer's isolation responsibility if they accept third-party `.fx`. **No longer a v1.0 blocker.** |
| 26 | ✅ Done (PR #18) | [DONE/PHASE-26-documentation-site.md](DONE/PHASE-26-documentation-site.md) | **DocFX → GitHub Pages documentation site** at `kaltinril.github.io/ShadowDusk/`: auto-generated API reference (from `///` doc-comments across the 4 libraries) + how-to/architecture/sample/CLI pages; `docs.yml` Pages workflow. Dev complete & merged. **One-time USER step remaining:** Settings → Pages → Source = GitHub Actions (then it auto-deploys). |
| 27 | ✅ Done (2026-06-12) | [DONE/PHASE-27-pre-1.0-test-and-verification-sweep.md](DONE/PHASE-27-pre-1.0-test-and-verification-sweep.md) | **Pre-1.0 test & verification sweep — complete.** Both halves merged same-day (PRs #63/#64): every deferred verify-and-tick item from Phases 2–9 & 15 run green and ticked; 32+ new tests landed (SD0101 negative coverage, `MgfxParameterMatch` golden vs the `mgfxc` goldens — which also pinned two render-proven, additive object-class divergences — GLSL Y-flip, `FileSystemIncludeResolver`/`DxcIncludeHandler`, direct `ShaderIRBuilder`); **CLI↔in-process byte-identity proven** over 27 fixture pairs via the new `InvocationMode` `[Theory]` (fixture duplication resolved on `CliBinaryFixture`); §9.4–9.6 pack/`--tool-path` install/self-contained publish verified + scripted (`tools/verify-cli-packaging.ps1`); all six 2026-06-12 review inputs closed (SD1902 e2e + per-path attribution fix; three explicit recorded decisions). One fidelity item re-deferred with full root-cause record: global-param default VALUES → [41](PHASE-41-fxc-oracle-monogame-fidelity.md). *(Promoted from Phase 100.)* |
| 28 | ✅ Done (PR #16) | [DONE/PHASE-28-vs-driven-monogame-effects.md](DONE/PHASE-28-vs-driven-monogame-effects.md) | **VS-driven MonoGame effects** — faithful compile+render of effects with their own vertex shader on the GL path (symmetric `vs_uniforms_vec4`, VS attribute/varying I/O, mat4 expansion); **rung-4 max-delta-0 vs `mgfxc` in real DesktopGL + WindowsDX** (oracle + vkd3d). All tasks/acceptance met. *(Promoted from backlog 17-VS.)* |
| 29 | Planned | [PHASE-29-mgcb-content-processor-plugin.md](PHASE-29-mgcb-content-processor-plugin.md) | **MGCB Tier-2 content-processor plugin** — promote the `ShadowDusk.MgcbPlugin` stub to a real MonoGame `EffectImporter`/`EffectProcessor` so MGCB invokes ShadowDusk natively in-process (vs the Tier-1 PATH/`ExternalTool` drop-in), wrapping `EffectCompiler` to emit the same `.mgfx`; a convenience over Tier-1, not required for the core promise. *(Promoted from Phase 100.)* |
| 30 | ✅ Done (2026-06-12) | [DONE/PHASE-30-ci-and-nuget-release.md](DONE/PHASE-30-ci-and-nuget-release.md) | **CI + NuGet release + `/release` automation.** (1) 3-OS CI; (2) **multi-package NuGet release** (`release.yml`) — packs & pushes **all six `ShadowDusk.*` packages + `mgfxc`** with a tag↔version guard; (3) the **`/release` skill** (§17). **As-built §18 (2026-06-07):** release.yml now has a **`pack-wasm` job** so a `/release` publishes **all six incl. self-contained `ShadowDusk.Wasm`** (the consumer "search→install→build a KNI/Blazor WASM project" goal — bundles `dxcompiler.wasm` as a Blazor static web asset, zero wiring); build-and-test **Windows-gated** so the narrow Phase-36 desktop-Linux DXC issue can't block. CLI POSIX-path fix (PR #19) prereq. **Both close-out items cleared (2026-06-12):** four releases published via `release.yml` (v0.1.1 → v0.4.0, all green) and the §16 browser smoke green since Phase 37 (PR #44). |
| 31 | Future | [PHASE-31-metal-msl-backend.md](PHASE-31-metal-msl-backend.md) | **Metal / MSL backend** — add the Metal Shading Language emission target (HLSL→DXC→SPIR-V→SPIRV-Cross **MSL**→MGFX, the GLSL branch's analogue; `SpvcBackend.Msl` already exists), replacing the empty `MslEmitter` stub and wiring `PlatformTarget.Metal` through the pipeline/CLI (today hard-rejected with `SD0200`). The hard part is the **validation surface**: no `mgfxc` MSL oracle and no MonoGame/KNI Metal runtime, so Metal ships **experimental/unvalidated** until a rung-4 render story lands — coordinated with Phase 30 macOS CI. *(Promoted from Phase 100.)* |
| 32 | Future (parked) | [PHASE-32-vulkan-backend.md](PHASE-32-vulkan-backend.md) | **Vulkan backend (SPIR-V target)** — wire the Vulkan SPIR-V `.mgfx` target end-to-end (DXC already emits SPIR-V; CLI/`PlatformTarget`/`MgfxProfile`/macros/flags already accept Vulkan; the real gap is routing Vulkan reflection through `SpirvReflector` instead of the empty-DXIL path). **Parked like 4.1**: no MonoGame/KNI Vulkan runtime and no `mgfxc`-Vulkan baseline, so "renders like `mgfxc`" is unreachable — ceiling is valid SPIR-V + well-formed `.mgfx`. *(Promoted from Phase 100.)* |
| 33 | ✅ Done (PR #9) | [DONE/PHASE-33-webgl2-es300-hidef-output.md](DONE/PHASE-33-webgl2-es300-hidef-output.md) | **KNI HiDef / WebGL2 (GLSL ES 3.00) compatibility** — fixes issue [#7](https://github.com/kaltinril/ShadowDusk/issues/7) (Victor Chelaru / XnaFiddle): a ShadowDusk `.mgfx` loads in KNI **Reach** but **fails in KNI HiDef** (WebGL2/ES-3.00: `gl_FragColor undeclared`). **Root cause verified 2026-06-03 — a one-spot fidelity gap, not a missing target:** `mgfxc` emits the PS colour output as `#define ps_oC0 gl_FragColor`, and KNI's HiDef runtime auto-converts *that* form to ES 3.00; ShadowDusk inlines a *raw* `gl_FragColor` ([MonoGameGlslRewriter.cs:246-247](../src/ShadowDusk.GLSL/MonoGameGlslRewriter.cs#L246-L247)) that the converter skips. Fix = emit `mgfxc`'s `#define` form → **one `.mgfx` works in Reach *and* HiDef, zero consumer input, no new flag/format, no KNI change**, strictly more `mgfxc`-faithful. *(Fidelity/reach track.)* |
| 34 | ✅ Done (PR #10) | [DONE/PHASE-34-gl-texture-breadth.md](DONE/PHASE-34-gl-texture-breadth.md) | **GL texture breadth** — lifted the Phase 33 `SD0210` guards by actually supporting the GL texture features the MojoShader rewriter didn't model. **Cube maps** (`TextureCube`): full support on Desktop + Reach + HiDef (legacy `samplerCube`/`textureCube`, sampler-`Type` byte 1; cross-validated vs the `mgfxc` cube golden). **3D textures + LOD/grad** (`Texture3D`, `SampleLevel`/`SampleGrad`): Desktop + HiDef via `texture3D` / generic `textureLod`/`textureGrad`; **Reach/WebGL1 = documented platform wall** (3D is WebGL2-only; fragment LOD is extension-gated — not compile-detectable in a single blob). The one structural change is the sampler-`Type` byte (was hardcoded 0). **Rung-4 render-validated** (cube/3D in real MonoGame DesktopGL; LOD/grad honored in the real GL driver); remaining gaps are platform/runtime/oracle walls, documented. 551/551 tests green. *(Fidelity/reach track; deferred from Phase 33.)* |
| 35 | 📋 Shell (not started) | [PHASE-35-forward-version-support.md](PHASE-35-forward-version-support.md) | **Forward-version support & validation (seamless)** — support newer versions with **zero consumer input** and *without* breaking backwards compat (**default stays MGFX v10 / MonoGame 3.8.2**; one output that just works old *and* new — never a flag/version the consumer must pick). Scaffold for future agents. Areas: **(A)** forward-compat validation of v10 output on MonoGame 3.8.4.1/3.8.5 *(doable now)* · **(B)** auto newer MGFX v11/KNIFX *(only if a runtime needs it)* · **(C)** DX12/DXIL render-validation *(gated on 3.8.5 stable)* · **(D)** un-park Vulkan (Phase 32) *(gated on 3.8.5 stable)*. *(Forward-compatibility track.)* |
| 36 | 🟠 Open → **absorbed by [37](DONE/PHASE-37-cross-platform-native-availability.md)** | [DONE/PHASE-36-dxc-linux-spirv-ice.md](DONE/PHASE-36-dxc-linux-spirv-ice.md) | **DXC `Internal Compiler error` on Linux.** ⚠️ Its "real ubuntu in-process compile works" conclusion is **DISPROVEN** — the `ci.yml` integration job (Release, in-process `EffectCompiler`) ICEs on ~120 ubuntu tests incl. `Minimal.fx`, while the *same run's* ImageTests pass 27/27. That paradox is now **Phase 37 → Finding B**; this doc is retained for the original Debug-CLI evidence + Vortice.Dxc native facts. |
| 37 | ✅ Done (2026-06-11) | [DONE/PHASE-37-cross-platform-native-availability.md](DONE/PHASE-37-cross-platform-native-availability.md) | **Cross-platform native availability — all three findings landed; `ci.yml` is green on all 3 OSes (first time ever) and the Browser render smoke passes (first time ever).** **(C ✅, PRs #35–#37)** all four pinned vkd3d 1.17 binaries hosted on `native-vkd3d-1.17` + restored/SHA-256-verified/packed under a hard release gate. **(B ✅, PR #42)** the Linux DXC ICE was Vortice.Dxc marshalling `LPCWSTR*` args as UTF-16 where `wchar_t` is 4-byte UTF-32 — fixed by `DxcNativeInterop` (raw vtable call, per-platform encoding; same pinned native); the "paradox" was headless ImageTests soft-skipping as PASS. **(A ✅ functionally, PR #41)** own pinned `libdxcompiler.dylib` (DXC `e043f4a1` = Vortice 3.3.4) built by `dxc-build.yml` for osx-x64/arm64, hosted on `native-dxc-1.7.2212.40`, restored under enforced pins, packed per-RID, loaded via Vortice's `Dxc.ResolveLibrary` event (its assembly already has a resolver — the doc's `SetDllImportResolver` plan would throw); macOS integration went ~124 `DllNotFoundException` → full suite green. **C-residue ✅ (PR #43, Phase 18 Track A):** pure-managed `RdefReader` replaces Windows-only D3DReflect (oracle-parity proven both backends, zero `.mgfx` byte change by full-corpus A/B) → DX11 end-to-end on any OS; DXC now lazily constructed. **Browser smoke ✅ (PR #44):** xvfb + llvmpipe for the DesktopGL RefRenderer + §6.1-calibrated tolerances → mode-1 10/10, HiDef guard GREEN. **Verification tail ✅ closed same day (PRs #46/#47/#48):** the cross-host **byte-identity manifest** (102 fixture×target entries, OpenGL+DX-vkd3d+FNA, macOS/Linux bytes proven == win-x64 — transfers the Windows rung-4 render proofs byte-for-byte); the 3-OS **pack→consume scratch-consumer** workflow (real-Mac `dotnet add package` → compile, plus the inline pack gate); **real-GL ImageTests on ubuntu** (xvfb/llvmpipe, 23 tests genuinely render) + **`SHADOWDUSK_REQUIRE_GL`** hard gate so headless soft-skips can never masquerade as passes (3 latent maskers fixed, incl. a GLFW registration race that hit real Windows hosts). Watch items recorded in the doc: a 1× macOS test-host crash flake (failed loudly — correct direction), Mesa `textureLod` legacy-dialect strictness (Phase 34 follow-up). |
| 38 | 🟢 Implemented (PR #25) | [PHASE-38-wasm-compile-diagnostics.md](PHASE-38-wasm-compile-diagnostics.md) | **Real line/column compile diagnostics from the in-browser (WASM) compiler** — bad HLSL in the browser now yields `file:line:col: error: message` instead of the opaque `[object WebAssembly.Exception]`; G1 byte-identity 10/10 preserved. Last rung (squiggle in a real KNI/Blazor browser) outstanding. |
| 39 | ✅ Done — rung 4 (2026-06-09) | [DONE/PHASE-39-fna-fx2-output-target.md](DONE/PHASE-39-fna-fx2-output-target.md) | **FNA fx_2_0 output target** — `PlatformTarget.Fna` compiles D3D9-style `.fx` → the legacy D3D9 Effects binary (`0xFEFF0901`, `.fxb`) FNA loads: vkd3d `D3D_BYTECODE` SM1–3 blobs (pinned 1.17) + new `Fx2EffectWriter`/`Fx2EffectBuilder`/`CtabReader`/`D3d9BytecodePatcher` per the MojoShader-derived spec `docs/fx2-binary-format.md`. **All four evidence rungs proven**: structural validation calibrated against `fxc /T fx_2_0` goldens, and `validation/FnaValidation` renders the corpus in **real FNA 26.06 pixel-equivalent to the fxc oracle (gate 14/14 — 10 PS-only + 4 VS-driven incl. multi-pass/in-pass-state coverage — + 12 extended, Δ ≤ 1/255)** — after fixing three vkd3d-vs-MojoShader codegen incompatibilities (texkill writemask, pre-SM3 texld swizzle, the ±2³² def-literal LLP64 print bug). **Self-contained NuGet proven for win-x64 + linux-x64** (`runtimes/<rid>/native` packing + loader probe; cross-host SHA256-identical output). Purely additive (existing GL/DX bytes verified unchanged vs main). Follow-ups **closed by Phase 37 C (2026-06-10)**: all four per-RID vkd3d binaries (incl. osx-x64/osx-arm64) hosted on `native-vkd3d-1.17` + auto-restored/packed; FNA compile suite (rungs 1–2) now green on ubuntu/macOS CI. |
| 40 | ✅ Done (2026-06-09) | [DONE/PHASE-40-fna-fidelity-hardening.md](DONE/PHASE-40-fna-fidelity-hardening.md) | **FNA fidelity hardening** — fixes every FNA-specific defect from the five-lens Phase-39 evaluation + 12-finding adversarial diff review: brace-form sampler blocks / paren texture refs (the silently-lost-binding HIGH, `ClipShaderNew` proven at rung 4), all 11 missing FNA-honored render states + loud SD0303 on FNA-throwing ones, stage/profile cross-checks (policy + writer version-token choke point), SM1 rejection, patcher pass-2 bounds + predicated SD0305 + SM3 texld-modifier routing, matrix parameter-class fxc-parity (pinned by the new `matrix.fxb` golden), SD0210/SD0300 guards made CI-visible, harness discrimination (distinct mask texture, param hit-set asymmetry notes, technique-by-name rows). **Gate 17/17, suite 851 green (+112 tests).** `release.yml` now fails red if the packed NuGet is missing vkd3d natives (a stopped release beats shipping the FNA target broken — Phase 37 C makes CI satisfy it). Deferred: macro-profile honoring, array-default propagation, float4x3 (non-square golden), semantics emission. |
| 41 | 🟢 Research largely complete (2026-06-12) | [PHASE-41-fxc-oracle-monogame-fidelity.md](PHASE-41-fxc-oracle-monogame-fidelity.md) | **`fxc.exe` oracle comparison & MonoGame fidelity matching.** Full-corpus structural divergence matrix (DX + GL) built/committed ([appendix](PHASE-41-appendix/structural-divergence-matrix.md)): **64/92 golden cells structurally clean**, the 7 divergences ALL known-render-equivalent (GL cbuffer sizing F4, anonymous-pass naming, annotation counts F2). **Found two real product gaps → each its own follow-up: GAP-1 (HIGH)** macro-defined techniques aren't detected (`FxPreParser` counts techniques pre-preprocess, so the `TECHNIQUE()` macro idiom is invisible) → **the MonoGame stock effects (BasicEffect/SkinnedEffect/EnvironmentMap/Penumbra*, 10 fixtures) fail SD0010**, all with goldens ready as a 20-cell rung-4 corpus; **GAP-2 (MEDIUM)** DeferredSprite fails on GL with a COLOR-semantic error. `d3dcompiler_47`-vs-`fxc` delta study deferred (low-value given the clean structural result). Research only — no product change made here. *(Fidelity/completeness track.)* |
| 4.1 | ✅ Done (2026-06-12) | [DONE/PHASE-4.1-SPIKE-wasm-directx-dxbc.md](DONE/PHASE-4.1-SPIKE-wasm-directx-dxbc.md) | **vkd3d-shader → WASM** — the keystone of the browser-export-station vision, landed: the pinned vkd3d 1.17 builds to WASM via emscripten 3.1.34 **with zero source patches** (`vkd3d-wasm-build.yml`; hosted on `native-vkd3d-wasm-1.17`, 1.27 MB raw / 0.43 MB gzip — an order of magnitude under budget), and `WasmVkd3dShaderCompiler` routes browser `PlatformTarget.DirectX` **and** `Fna` through it (SD0304 guard flipped; loud SD1902 only when the module is genuinely absent). **Byte-identity gate PASSED 98/98** (37 DX SM5 + 28 FNA SM1–3 fixtures — WASM bytes == desktop bytes through the product shim, the G1 pattern); restore pins real, `VerifyVkd3dWasmPresent`/release-gates ensure a nupkg can never ship without it. ONE wasm artifact closed BOTH browser cells (the fx_2_0 writer/patcher/reflection are managed C# that already ran in WASM; same pinned source ⇒ no substitute compiler). **G2 analogue closed (2026-06-12): real-browser proof PASSED 65/65** — headless Chromium runs the real `WasmShaderCompiler` (real vkd3d-shader.wasm fetch) over the FULL DX+FNA byte-identity corpus and every artifact SHA-256-matches the committed cross-host manifest; since the browser cannot render DXBC/D3D9 (no Direct3D in a browser), render-equivalence closes by **transitivity** (browser bytes == manifest bytes == desktop rung-4 render-proven bytes, Phases 18/39–40) — `browser-vkd3d-gate.mjs` in `wasm.yml`, verdict in `RESULTS-VKD3D-BROWSER.md`. Housekeeping: the temporary `push:` trigger was removed (PR #54); doc could move to `plan/DONE/`. Server-relay stays **out of bounds** for the product (a consumer hosting the library server-side is their own legitimate choice). |
| 42 | ✅ Done (2026-06-12, PR #58) | [DONE/PHASE-42-sync-compile-api.md](DONE/PHASE-42-sync-compile-api.md) | **`InitializeAsync()` + synchronous `Compile()` (issue #28**, vchelaru — the linchpin for `XnaFiddle#39` runtime shader compilation from synchronous call sites like `Content.Load<Effect>`). The pipeline core (`CompilationPipeline.Run`) is now **synchronous end-to-end** (his verified key finding: per-compile work was already sync everywhere; the only genuinely-async work is the one-time WASM module load) and `CompileAsync` everywhere is a thin shell over that ONE core — never a second pipeline, so sync/async bytes are identical **by construction and by gate** (full corpus × OpenGL/DX/FNA, `SyncCompileByteIdentityTests`). On WASM, `InitializeAsync` eagerly warms all three modules (DXC + SPIRV-Cross + vkd3d — seamless: after one await, `Compile` of ANY target just works); a cold sync `Compile` returns the clear **SD1903** "await InitializeAsync() first" error, never an opaque abort — proven in a real browser (`browser-vkd3d-gate.mjs` Phase-42 section: cold SD1903 3/3, init idempotent, warm sync Compile 65/65 == committed manifest). Desktop `InitializeAsync` is a documented no-op. Suite 933 green; node vkd3d gate 98/98; grep-proof no `.Result`/`.Wait()` in the sync path. |
| 43 | ✅ Done (2026-06-12) | [DONE/PHASE-43-mgfx-writer-and-gl-uniform-fidelity.md](DONE/PHASE-43-mgfx-writer-and-gl-uniform-fidelity.md) | **MGFX writer & GL uniform-model fidelity — all ten findings closed same-day in three waves** (43A #73 writer formats, 43B #72 posFixup/LOD/VS-samplers, 43C #74 cbuffer/array model), every fix validated at the full ladder: mgfxc-3.8.2.1105-golden structural match, **real MonoGame 3.8.2 `Effect` load + pixel-identical render (maxDelta 0)** incl. the backbuffer case that hid the posFixup bug, manifest regen, FNA proven untouched (17/17 live). Highlights: pass render-states/annotations/sampler-states now byte-faithful to the real reader; the dynamic `posFixup` contract; the Mesa `textureLod` watch item CLOSED (guarded MojoShader header, ubuntu lane exclusion removed and passing); per-stage merged cbuffer records + recursive array `Elements` on every target; unmodelled shapes fail loudly (SD0210/SD0012, registered). Bonus finding: mgfxc+MonoGame GL is itself broken for statically-partially-read uniform arrays (MojoShader register compaction vs full-layout records) — ShadowDusk deliberately emits the correct full layout, recorded in `docs/glsl-uniform-naming.md`. The proxy trap is structurally closed: the corpus now permanently covers every shape that hid the seven HIGHs. Carried follow-ups: int/bool/mat3/struct uniform members (loud SD0210, staged), F11 per-OS DXC compile divergence, `ColorWriteEnable` flag tokens, full `tex2Dlod` forwarding. |
| 99 | 🔬 Open (investigation) | [PHASE-99-investigation-items.md](PHASE-99-investigation-items.md) | **Investigation-grade backlog from the 2026-06-13 project review** (the safe review fixes already landed in PR #80). Two deferred items, each post-1.0 and non-blocking, that need their own scoped/validated effort: **INV-1** per-pass shader memoization — first INVESTIGATE whether `mgfxc` dedups shared entry points (the byte-identity corpus matching goldens without dedup suggests it may NOT, in which case deduping the shader table would be a fidelity *regression*, not a win); decision tree drives whether it is an output-changing dedup, an output-identical internal cache, or closed. **INV-2** decompose the two giant core methods (`CompilationPipeline.Run` ~600 lines, `MonoGameGlslRewriter.Rewrite` ~540 lines) incrementally, behavior-preserving, validated by full-suite + byte-identity zero-churn after each extraction. *(Distinct from the retired Phase 100.)* |
| 100 | ✅ Retired | [PHASE-100-deferred-backlog.md](PHASE-100-deferred-backlog.md) | **Emptied & retired (2026-06-03).** Every item was promoted into a real phase: test/verification sweep → [27](DONE/PHASE-27-pre-1.0-test-and-verification-sweep.md), VS-driven effects → [28](DONE/PHASE-28-vs-driven-monogame-effects.md), MGCB Tier-2 plugin → [29](PHASE-29-mgcb-content-processor-plugin.md), Metal/MSL → [31](PHASE-31-metal-msl-backend.md), Vulkan → [32](PHASE-32-vulkan-backend.md) (the WASM browser-runtime tail had already moved to 23/24/30 §16). Kept only as a breadcrumb so old links resolve — the deferred bucket is no longer a parking lot |

### Roadmap tracks (open phases)

The remaining open phases group into four tracks (the numbers are stable IDs, not a sequence):

- **Release (→ v1.0):** [25](PHASE-25-security-hardening.md) security (reframed doc-only — **not a v1.0 blocker**; just write `SECURITY.md`) · [26](DONE/PHASE-26-documentation-site.md) docs site · [27](DONE/PHASE-27-pre-1.0-test-and-verification-sweep.md) ✅ test & verification sweep — done 2026-06-12 · [30](DONE/PHASE-30-ci-and-nuget-release.md) cross-platform CI **+ NuGet release + `/release` skill (§17)**. The path from "works here" to a trustworthy public 1.0 — and the one-command cut that ships it.
- **Fidelity / completeness:** [43](DONE/PHASE-43-mgfx-writer-and-gl-uniform-fidelity.md) ✅ MGFX-writer & GL uniform-model fidelity — the review's seven beyond-corpus HIGHs all closed at the full validation ladder (done 2026-06-12) · [28](DONE/PHASE-28-vs-driven-monogame-effects.md) ✅ VS-driven effects — compile the common custom-vertex-shader case, not just PS-only · [33](DONE/PHASE-33-webgl2-es300-hidef-output.md) ✅ KNI HiDef/WebGL2 — close the `gl_FragColor` fidelity gap so one `.mgfx` works in KNI Reach *and* HiDef · [34](DONE/PHASE-34-gl-texture-breadth.md) ✅ GL texture breadth — cube maps everywhere; 3D/LOD on Desktop+HiDef (WebGL1 can't, by platform limit) · [41](PHASE-41-fxc-oracle-monogame-fidelity.md) `fxc.exe` oracle comparison — DX11 + GL full-corpus fidelity matrix vs genuine `fxc`/`mgfxc` now that a local `fxc.exe` is available (the FNA fx_2_0 oracle is already covered by Phases 39–40).
- **Delivery shapes:** [29](PHASE-29-mgcb-content-processor-plugin.md) MGCB Tier-2 content-processor plugin — native MGCB integration beyond the Tier-1 drop-in · [42](DONE/PHASE-42-sync-compile-api.md) ✅ `InitializeAsync()` + synchronous `Compile()` — the API shape for synchronous call sites (`Content.Load<Effect>`, single-threaded Blazor WASM; issue #28, the `XnaFiddle#39` linchpin).
- **Backend breadth (post-1.0, validation-gated):** [31](PHASE-31-metal-msl-backend.md) Metal/MSL · [32](PHASE-32-vulkan-backend.md) Vulkan (parked) · [39](DONE/PHASE-39-fna-fx2-output-target.md) ✅ FNA fx_2_0 (all four rungs proven, PS-only + VS-driven — real FNA renders pixel-identical to fxc; vkd3d natives for all four RIDs ship since Phase 37 C) · [4.1](DONE/PHASE-4.1-SPIKE-wasm-directx-dxbc.md) ✅ DirectX/FNA-in-WASM (vkd3d→WASM; all gates closed incl. the real-browser G2 byte-identity proof).
- **Forward-compatibility (newer versions, seamless):** [35](PHASE-35-forward-version-support.md) forward-version support — validate v10 output on newer MonoGame (3.8.4.1/3.8.5); auto newer-format emission if ever needed (note: MonoGame `MGFX`-v11 and KNI **KNIFX** are two distinct formats, not one byte bump — [appendix](PHASE-35-appendix/knifx-vs-mgfx-v11-research.md)), DX12/DXIL render-validation, Vulkan un-park (the last two 3.8.5-gated). **Seamless (zero consumer input) + backwards-compatible — v10 / MonoGame 3.8.2 stays the default; the consumer never picks a version.**

**Investigation backlog:** [Phase 99](PHASE-99-investigation-items.md) holds post-1.0 investigation-grade items surfaced by the 2026-06-13 project review (INV-1 shader memoization fidelity question; INV-2 decompose the two giant core methods) — non-blocking, each needs its own scoped/validated effort.

[Phase 100](PHASE-100-deferred-backlog.md) is now retired/emptied — its items were promoted into the phases above. The deferred bucket is no longer a forever-parking-lot.

---

## Dependencies

```
Phase 1  (scaffold)
  └─ Phase 2  (FX9 parser)
  └─ Phase 3  (preprocessor)
       └─ Phase 4  (DXC integration)
            │    └─ Phase 4.1 (SPIKE: WASM + DirectX DXBC — parked, far-future)
            └─ Phase 5  (reflection)
            └─ Phase 6  (SPIRV-Cross transpilation)
                 └─ Phase 7  (binary writer)
                      └─ Phase 8  (ShadowDusk.Compiler library — EffectCompiler NuGet)
                           ├─ Phase 9  (CLI — ShadowDusk.Cli dotnet tool)
                           │    └─ Phase 15 (integration tests)
                           │         ├─ Phase 16 (image regression)
                           │         │    └─ Phase 17 (MonoGame runtime equivalence — OpenGL)
                           │         │         ├─ Phase 18 (DirectX DXBC — WindowsDX fidelity)
                           │         │         └─ Phase 19 (WASM runtime compilation — was "9W")
                           │         └─ Phase 30 (CI — desktop matrix + WASM/browser §16)
                           └─ Phase 19 (WASM — ShadowDusk.Wasm JS interop impl)
                                └─ Phase 22 (KNI Blazor-WASM SAMPLE app)
                                     └─ Phase 24 (browser render validation — Playwright; run FIRST)
                                          └─ Phase 23 (FAITHFUL in-browser compile — DXC→WASM, Option A)
```

> **The WASM-KNI product spine (the goal "a user uses our library inside WASM KNI"):** Phase 19 (engine) → Phase 22 (sample proves the shape) → **Phase 24** (real-browser render proof — retires the KNI MGFXReader10/v11 load risk; deliberately *before* the build effort) → **Phase 23** (faithful DXC→WASM frontend — the product). Phase 23's Gate 3 reuses Phase 24's harness; **Phase 30 §16** wires both into CI.
>
> Phase 19 intentionally appears twice (a diamond): it depends on **Phase 8** (the `IShaderCompiler` abstraction it implements) *and* on **Phase 17** (the MonoGame-loadable `.mgfx` format + MojoShader-dialect GLSL a browser-produced effect must also carry). Phases **23/24** (not the already-done Phase 19) additionally depend on **Phase 25** (untrusted-input security) and **Phase 30** (CI), which the graph keeps off the spine for readability — see each phase doc's "Depends on" line for the full set. Phase 19 supersedes the earlier "9W" placeholder.

## Key Decisions Already Made

- **Option B pipeline:** DXC → SPIR-V → SPIRV-Cross → GLSL/MSL. Option A (FXC/MojoShader) rejected — requires Wine on Linux/macOS.
- **DXC wrapper:** `Vortice.Dxc` NuGet package (bundles prebuilt native binaries for all platforms).
- **SPIRV-Cross binding:** Raw P/Invoke against the SPIRV-Cross C API (not Veldrid.SPIRV).
- **Default MGFXVersion:** `10` (MonoGame 3.8.2 stable) — the one format both MonoGame and KNI load. The `--mgfx-version 11` flag is a **header-byte escape hatch only, not a faithful v11/KNIFX writer** (it bumps the byte over a v10 body; DOA in KNI). "v11" is in fact two distinct formats (MonoGame `MGFX`-v11 vs KNI **KNIFX**, separate signatures) — see [Phase 35 appendix](PHASE-35-appendix/knifx-vs-mgfx-v11-research.md).
- **Metal scope:** Out of scope until the OpenGL path is working and validated.
- **MGCB integration:** Tier 1 only (PATH-based drop-in binary named `mgfxc`). Tier 2 content processor plugin is a separate future undertaking.
- **Dual delivery targets:** CLI (`ShadowDusk.Cli`) and WASM library (`ShadowDusk.Wasm`). Output is always a `.mgfx` blob; `IShaderCompiler.CompileAsync` abstracts the difference. WASM implementation uses JS interop to call WASM-compiled DXC and SPIRV-Cross.
- **`.mgfx`, not `.xnb`, from core (2026-06-08):** ShadowDusk (library + CLI) emits the **raw `.mgfx`** effect blob, matching `mgfxc` — it does **not** wrap output in the Content Pipeline `.xnb` container. `.xnb` wrapping is the content-pipeline layer's job and its home is **[Phase 29](PHASE-29-mgcb-content-processor-plugin.md)** (MGCB Tier-2 plugin → MonoGame's own `ContentWriter`). A hand-rolled XNB writer in core was considered and rejected: it would be the first divergence from `mgfxc` parity and would duplicate/risk-desync Phase 29's validated envelope. Consumers loading via `new Effect(gd, bytes)` use the `.mgfx` directly; those needing `Content.Load<Effect>` build through MGCB (or wrap the bytes themselves). Revisit only as a deliberate, explicitly-scoped feature if a downstream (e.g. XnaFiddle project export) needs pre-wrapped `.xnb`. (FNA's `PlatformTarget.Fna` is the one non-`.mgfx` output — the raw `.fxb` effect binary, likewise unwrapped.) User-facing explanation: `docfx/guides/choosing-a-target.md` + `parameters-and-caveats.md`.
- **FNA target (Phase 39):** `PlatformTarget.Fna` emits the legacy D3D9 fx_2_0 effects binary (`.fxb`) FNA/MojoShader loads — vkd3d-shader `D3D_BYTECODE` SM1–3 blobs (same pinned 1.17 binary as the DX backend; **no version bump** — empirically sufficient) wrapped by ShadowDusk's own `Fx2EffectWriter` (MojoShader's parser is the byte spec, `docs/fx2-binary-format.md`). Always vkd3d on every host (never the d3dcompiler oracle ⇒ host-independent bytes); `fxc /T fx_2_0` is a Windows test-oracle only. Purely additive — the fully separate `RunFnaAsync` pipeline cannot change existing GL/DX output. Option B (waiting for upstream vkd3d's stubbed fx_2_0 writer) stays a background watch.
- **Compiler-leverage strategy (owner-directed, 2026-06-09):** ShadowDusk **never owns
  compiler internals** — HLSL compilation is DXC + vkd3d-shader, cross-compilation is
  SPIRV-Cross; we own only the container writers (`.mgfx`/`.fxb`), the runtime-dialect
  adapters, and the **real-runtime validation that is the actual moat**. Upstream gaps get
  loud failures or minimal byte-tested patches (the `D3d9BytecodePatcher` pattern) plus an
  upstream-fix follow-up — never a fork. Full statement + the host×target matrix:
  `docs/the-purpose.md` → *Compiler-leverage strategy*.
- **Browser = export station (owner-directed, 2026-06-09):** compile-target ≠
  render-backend; every host can emit every target whose upstream compilers exist for that
  host, with a host-appropriate default and an explicit override (the allowed kind of
  choice). A `ShadowDusk.Wasm` website should let users upload `.fx` and download
  MonoGame GL/DX **and FNA** artifacts, byte-identical to desktop output. The single
  keystone is **vkd3d→WASM (Phase 4.1, un-parked)** — it closes both the DX and FNA
  browser cells at once; the rest of those pipelines is managed code that already runs in
  WASM. Completion order: 37 C (+ macOS vkd3d) ✅ done 2026-06-10 → 37 A/B → 4.1 →
  Metal/Vulkan stay validation-gated.
- **KNI compatibility:** KNI uses the same `.mgfx` format as MonoGame. No special output path needed for KNI — **with one caveat (Phase 33):** KNI's **HiDef** profile uses a WebGL2 / GLSL ES 3.00 context and relies on its runtime converter (`ConvertGLSLToGLSL300es`) rewriting `mgfxc`'s `#define ps_oC0 gl_FragColor` form to `out vec4`. ShadowDusk must emit that exact `#define` form (not a raw `gl_FragColor` write) for the same `.mgfx` to load in both Reach and HiDef. See [Phase 33](DONE/PHASE-33-webgl2-es300-hidef-output.md).

---

## ✅ Resolved Constraint (Phase 18): DXC Cannot Produce SM5 DXBC

Discovered during Phase 4 implementation. **DXC only produces SM6 DXIL** — it rejects `vs_5_0`/`ps_5_0` profiles with `error: invalid profile` for non-SPIRV targets. It has never supported DXBC (SM1–SM5) output. **Phase 18 (done 2026-05-30) resolved this** by routing the DX11 path through a DXBC backend behind `IDxbcShaderCompiler` — the cross-platform **vkd3d-shader** library (HLSL → DXBC_TPF), validated 10/10 in real MonoGame WindowsDX, with `d3dcompiler_47.dll` as a Windows-only correctness oracle. DXC's DXIL path is retained only for DX12/KNI. The historical analysis below is kept for context.

**Impact by target:**

| Target | Status | Notes |
|--------|--------|-------|
| OpenGL (SPIR-V path) | ✅ Unaffected | DXC compiles `vs_5_0 -spirv` fine; SPIRV-Cross handles the rest |
| Vulkan (SPIR-V path) | ✅ Unaffected | Same pipeline |
| DirectX 11 | ✅ Resolved (Phase 18) | DXC's SM6 DXIL won't load on D3D11; **Phase 18 routes DX11 through vkd3d-shader → SM5 DXBC** (`d3dcompiler_47` oracle on Windows). 10/10 load + render pixel-equivalent in real WindowsDX |
| DirectX 12 / KNI | ✅ Works | D3D12 natively accepts DXIL (SM6); `vs_6_0`/`ps_6_0` used in Phase 4 |

**SM6 DXIL on D3D11 is a hard no.** `ID3D11Device::CreateVertexShader` rejects DXIL unconditionally — even on Windows 10. DXIL is a D3D12-only format.

### Cross-Platform SM5 DXBC Options

The only viable cross-platform HLSL→DXBC compiler that does not require Wine:

**`vkd3d-shader`** (`gitlab.winehq.org/wine/vkd3d`)
- A standalone C library — **no Wine runtime required**. It is developed under the Wine project umbrella but ships and runs independently; linking against it is identical to linking SPIRV-Cross.
- Compiles HLSL to SM4/SM5 DXBC via a `vkd3d_shader_compile()` C API; P/Invoke pattern mirrors SPIRV-Cross
- Cross-platform: Linux, macOS, Windows. **No official prebuilt Windows DLL exists** (Phase 18 finding) — we build `libvkd3d-shader` from WineHQ source (vkd3d-1.17 via MSYS2/autotools, self-contained, zero non-system deps) and host per-RID; Linux/macOS have distro/source builds. `tools/restore.*` carries the recipe.
- License: **LGPL-2.1+** — safe to use as a dynamically-linked native binary (same model as SPIRV-Cross)
- Active development; v2.0 released May 2026; adopted by SDL3's `SDL_shadercross` as its non-Windows DXBC backend (no Wine involved there either)
- Coverage for MonoGame-style effects (cbuffers, Texture2D samplers, VS/PS only): sufficient
- Known gaps: SM5 UAV buffers (RWBuffer) and tessellation stages are partial; irrelevant for basic MonoGame effects
- **Not byte-identical to `fxc.exe` output**, but semantically equivalent; on Windows `d3dcompiler_47.dll` (ships with Windows) can serve as a fidelity fallback

**Recommended path for DirectX SM5 support (Phase 4.1):**

No end-user installation required on any platform:

| Platform | Library | How it's delivered |
|----------|---------|-------------------|
| Windows | `libvkd3d-shader.dll` (shipping) + `d3dcompiler_47.dll` (oracle) | vkd3d built/hosted per-RID (the cross-platform shipping backend on every OS); `d3dcompiler_47.dll` ships with Windows and is used only as the correctness oracle |
| Linux | `libvkd3d-shader.so` | Downloaded by `tools/restore.sh`, bundled into `dotnet publish` output |
| macOS | `libvkd3d-shader.dylib` | Same as Linux |

This mirrors exactly how SPIRV-Cross is distributed today. Users install ShadowDusk and nothing else.

> **WASM delivery target: DirectX DXBC is an open problem.** Native P/Invoke is unavailable in the browser, so neither `d3dcompiler_47.dll` nor `libvkd3d-shader` can be called directly from .NET WASM. No prebuilt WASM artifact of vkd3d-shader currently exists. The path forward requires one of: (a) compiling vkd3d-shader to WASM via emscripten and calling it via `[JSImport]` (same pattern as WASM-compiled DXC), or (b) a server-side compilation relay for DXBC. This is unresolved — see [DONE/PHASE-4.1-SPIKE-wasm-directx-dxbc.md](DONE/PHASE-4.1-SPIKE-wasm-directx-dxbc.md) for the full problem statement and candidate solutions.

**As of Phase 18 (done 2026-05-30), the `PlatformTarget.DirectX` DX11 profile compiles to SM5 DXBC via vkd3d-shader** and loads/renders in real MonoGame WindowsDX (10/10; `d3dcompiler_47` oracle on Windows). DXC's SM6 DXIL path is retained only for DX12/KNI. The OpenGL path (Phase 17) is fully functional. **WASM + DXBC remains the open problem** (Phase 4.1) — no native P/Invoke in the browser.
