# Phase 33 — issue #7 GREEN validation (KNI HiDef / WebGL2 / GLSL ES 3.00)

_Generated by `run-harness.mjs --corpus=sd-hidef` — headless Chromium (ANGLE/SwiftShader), KNI **HiDef** (`?profile=hidef` → WebGL2 → GLSL ES 3.00). ShadowDusk-compiled `.mgfx`._

## Verdict: GREEN — 10/10 corpus shaders load + render in KNI HiDef

Every shader's ShadowDusk `.mgfx` loaded with no GLSL compile error in the KNI HiDef / WebGL2 context and rendered within tolerance vs its Reach render — issue #7 is fixed.

## Per-shader mode-1 results (HiDef / WebGL2)

Reference = the SAME ShadowDusk `.mgfx` bytes rendered on desktop **DesktopGL** (`references-sd/`), so a render diff isolates KNI's ES-3.00 conversion of our bytes.

| Shader | KNI HiDef load | GLSL error (thrown / console) | Render | Max delta | Verdict |
|---|---|---|---|---|---|
| Grayscale | OK | — | OK | 1 | PASS(tol) |
| Invert | OK | — | OK | 1 | PASS(tol) |
| TintShader | OK | — | OK | 1 | PASS(tol) |
| Sepia | OK | — | OK | 1 | PASS(tol) |
| Saturate | OK | — | OK | 3 | PASS(tol) |
| Pixelated | OK | — | OK | 1 | PASS(tol) |
| Scanlines | OK | — | OK | 1 | PASS(tol) |
| Fading | OK | — | OK | 1 | PASS(tol) |
| Dots | OK | — | OK | 11 | PASS(tol) |
| Dissolve | OK | — | OK | 128 | PASS(tol) |

## Dissolve & the mediump→highp watch-item (documented, not masked)

The task flagged that KNI's converter forces `precision mediump`→`highp` under HiDef, which *could* shift Dissolve's data-dependent `discard`/threshold boundary vs the Reach (`mediump`) render. **Verified it did NOT:** the per-shader deltas from this HiDef run are **byte-for-byte identical** to the Reach run (`--corpus=sd`, same bytes, WebGL1) — Dissolve is `maxDelta=128` over `380/262144` px (0.145%) in **both** profiles; Dots `11`, Saturate `3`, all others `1` in **both**. So:

- **No mediump→highp shift on Dissolve** — HiDef and Reach agree exactly. (Consistent with the rewriter's existing note that the headless ANGLE/SwiftShader GL evaluates `mediump` and `highp` identically.)
- Dissolve's `128`-LSB delta on 0.145% of pixels is the **pre-existing Phase 24 caveat**: its `discard`+threshold band lands on slightly different texels in WebGL vs DesktopGL of the *same* bytes — a localized boundary difference, **under** the harness's 0.5%-of-pixels budget, **not** a structural divergence and **not** introduced by this fix. Documented here, not masked.

This run compares HiDef/WebGL2 against the **DesktopGL** render of the same bytes (`references-sd/`), so the deltas isolate "KNI's ES-3.00 conversion of our bytes," exactly as intended.

> Note: this section is re-added after each harness run (the harness regenerates the rest of this file).

## How to re-run (RED before the fix, GREEN after)

```bash
cd tests/ShadowDusk.BrowserTests
npm install                              # playwright + pngjs
npx playwright install chromium          # CI: --with-deps chromium
node publish-sample-sd-hidef.mjs         # publish + compile corpus w/ ShadowDusk + render Reach refs
node run-harness.mjs --corpus=sd-hidef   # load each .mgfx in KNI HiDef/WebGL2
```

Before the `MonoGameGlslRewriter` `#define` fix this writes `RESULTS-SD-HIDEF-REPRO.md` (RED — `Grayscale` fails to load). After the fix it writes `RESULTS-SD-HIDEF.md` (GREEN — all load + render).

## Environment (this run)

- **Browser harness ran end-to-end here:** YES — `node` + Playwright Chromium + the `dotnet publish` of the `net8.0-browser` WASM sample all succeeded. The KNI game booted in HiDef and KNI requested a real **WebGL2 / GLSL ES 3.0** context (verified: `WebGL 2.0 (OpenGL ES 3.0 Chromium)` under ANGLE/SwiftShader). The errors above are from that real runtime, not a static inspection.
- **Determinism:** software GL (`--use-gl=angle --use-angle=swiftshader`) — no GPU required, so CI reproduces it.
- **To run in CI (Phase 30 §16):** `dotnet workload install wasm-tools`, then in `tests/ShadowDusk.BrowserTests`: `npm install` (no lockfile, so not `npm ci`), `npx playwright install --with-deps chromium`, `node publish-sample-sd-hidef.mjs`, `node run-harness.mjs --corpus=sd-hidef`.

